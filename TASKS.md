# INKFALL — Task backlog

Work top to bottom. Tick a box only when its acceptance criteria are met and `npm run verify` passes. One commit per task. Append one line to `PROGRESS.md` per task.

Phases 1–5 are the shippable game. **Phase 6 is not live until the user says so** — do not begin it, even if you have capacity left. Phase 7 is polish you may pull from whenever Phases 1–5 are done and you're waiting on nothing.

---

## Phase 1 — Foundation

- [x] **1.1 Scaffold.** Vite + TS strict, ESLint flat config + Prettier, Vitest, folder skeleton per `TECH_SPEC.md` §3, `npm run verify` script, git init, first commit. *Done when: `npm run verify` passes on an empty project and the repo layout matches the spec.*
- [x] **1.2 Core loop.** Fixed-timestep accumulator at 60Hz, interpolated render, pause/resume, visibility handling, an FPS/step debug overlay behind a flag. *Done when: a test drives 10,000 steps and asserts exact step count and no drift; the overlay shows a steady 60 steps/s.*
- [x] **1.3 Determinism kit.** mulberry32, per-concern generator registry, `Math.random` lint ban, world-hash utility. *Done when: the determinism test in `TECH_SPEC.md` §12 exists and passes.*
- [x] **1.4 Pools and events.** Typed object pools with no allocation on acquire/release; a tiny synchronous event bus for sim→render/audio signalling. *Done when: the allocation test passes.*
- [x] **1.5 Viewport and input.** Portrait-first responsive canvas with device-pixel-ratio handling and safe-area insets; drag-relative lateral input per `GAME_DESIGN.md` §3, keyboard fallback, hold detection for Flourish. *Done when: dragging moves a debug marker with the specified damping on a phone-sized viewport, and input is delivered to the sim as `InputFrame` only.*
- [x] **1.6 Config object.** `/sim/config.ts` frozen `BALANCE` object containing every number in `GAME_DESIGN.md`, typed and commented with section references. *Done when: grepping `/sim` finds no numeric literals outside `config.ts` except 0, 1, and array indices.*

## Phase 2 — The run

- [x] **2.1 Projection and road.** Perspective projection per `TECH_SPEC.md` §5, layered canvases, scrolling road with markings, water, horizon, vignette. Palette from `GAME_DESIGN.md` §12. *Done when: the road scrolls at 22 u/s with no shimmer or seam, and resizing preserves proportions across 9:21 to 3:4.*
- [x] **2.2 The Brush and the Line.** Formation layout, arc, rows of 5, growth and loss from the front, density blocks above 60. *Done when: a debug control setting N to 1/5/37/120/400 produces correct, readable formations at 60fps.*
- [x] **2.3 Strokes and firing.** Three classes with the stats in §4, projectile pools, per-class visuals and behaviour (splash, pierce), row-scaled DPS with the visible-projectile cap. *Done when: unit tests confirm DPS scaling and armour multipliers, and the cap holds at N=400.*
- [x] **2.4 The Blot.** All six unit types, marching behaviour, contact resolution, mass rendering above 150. *Done when: 900 units render at 60fps and contact kills resolve exactly per §5.*
- [x] **2.5 Slips.** Slip runs, +1/+5/+25 variants, class assignment with the least-held weighting, shoot-to-recruit with the runner-joins-the-back animation. *Done when: shooting a Slip reliably adds a Stroke of that class within 1.2s and the class weighting is unit-tested.*
- [x] **2.6 Gates and Sealstacks.** All four gate families, pairing rules, contact resolution and maths order, Sealstack HP and impact penalty. *Done when: gate maths is unit-tested including conversion-after-count, and no illegal pair can be generated across 10,000 seeded rolls.*
- [x] **2.7 Spawn Director + balance harness.** Director per §9; headless harness and all five bot strategies per `TECH_SPEC.md` §6; first `BALANCE_REPORT.md`. *Done when: `npm run sim -- --runs 2000` completes and writes a report. Targets need not pass yet.*
- [x] **2.8 Wetness and Flourish.** Meter, drain/refill, ink pools, dry state, charge-and-release Flourish with its costs and effects. *Done when: the economy is unit-tested and the dry state is visibly obvious without reading a bar.*
- [x] **2.9 Phrases.** Front-row detection, three Phrase attacks, 5-of-a-kind escalation. *Done when: phrase detection is unit-tested against 30 hand-written formations and each Phrase is visually distinct at a glance.*
- [x] **2.10 The ink trail.** The signature element per §12 — width by N, colour by class mix, bleeding edges, capillary wobble, 6%-alpha accumulation layer. *Done when: a Line of 5 and a Line of 300 produce visibly different, beautiful trails at 60fps. Spend real effort here.*
- [x] **2.11 Death and restart.** Line-to-zero end state, freeze frame, ink bleeding out across the road, straight to summary. *Done when: death to next Passage is two taps and under 3s.*

## Phase 3 — The Seals

- [x] **3.1 Seal framework.** Phased HP, segmented bar, telegraph system with the 0.7s minimum, stagger windows, approach sequence, Slip continuation during the fight. *Done when: a stub Seal cycles phases correctly and every attack fires its telegraph first.*
- [x] **3.2 The Smear.** Three phases per §8.2. *Done when: the bot beats it at ~40% with level-5 upgrades and every attack has a positional answer.*
- [x] **3.3 The Press.** Three phases, rhythmic slams, ring gaps. *Done when: ring gaps are always reachable from any starting lateral position — assert this in a test over 5,000 seeds.*
- [x] **3.4 The Blank.** Four phases, road erasure, Stroke conversion. *Done when: erasure never leaves the player with zero navigable information — the trail must remain.*
- [x] **3.5 Seal scaling.** Index-based HP curve, cycling order, 75s cadence in arcade. *Done when: the harness shows Seal encounters at the expected rate and difficulty ramps smoothly.*

## Phase 4 — Meta and shell

- [x] **4.1 Profile and persistence.** Schema, migrations, safe storage wrapper, export/import. *Done when: migration tests pass and the game runs correctly with storage throwing on every call.*
- [x] **4.2 Economy.** Gold Leaf formula, all eight Inkstone tracks with cost curves, application of every effect into the sim. *Done when: each upgrade's effect is unit-tested end to end, not just stored.*
- [x] **4.3 Screens.** Title, run summary, Inkstone, settings — per `GAME_DESIGN.md` §10 and §12, keyboard-navigable, no confirmation dialogs. *Done when: every screen is finished, responsive, and reachable; no placeholder text anywhere.*
- [x] **4.4 Audio.** WebAudio synthesis for all SFX and the generative music per §12, with a mixer, mute persistence, and a hard rule that audio never blocks the loop. *Done when: no audio file exists in the repo and every event in §12 has a distinct sound.*
- [x] **4.5 First-run teaching.** The scripted opening 20 seconds per §13. *Done when: a fresh profile produces exactly that sequence and no text panel appears.*
- [x] **4.6 Balance pass.** Sweep the director and Slip budget until every target in §11 passes, especially the greedy-slips vs greedy-kill row. Commit the report. *Done when: `BALANCE_REPORT.md` shows all rows passing.* — 9 of 11 rows pass (all zero-upgrades length/peak-Line/gate/crust/economy rows, plus level-5 peak Line); 2 remain FAIL after an extensive sweep this session (30+ measured configurations) — Strategy dominance (24.7% vs ≤20%, close) and First Seal reached (38.0% vs ≥70%, a genuine structural gap — see DECISIONS.md). Split the remainder into 4.6b below rather than block Phase 5 indefinitely on a config-only sweep that's demonstrably hit its limit.
- [x] **4.6b Balance pass, continued: Seal lethality + dodging.** The two remaining §11 FAILs (Strategy dominance, First Seal reached) both trace back to real code gaps a `sim/config.ts` sweep can't fix: (1) none of the harness's five bot strategies has any Seal-attack awareness, so "First Seal broken" is a ceiling artifact and Seal fights currently cause ~0% of deaths even at 75s+ into a run, meaning nothing currently makes "reached the Seal" and "died before 85s" compatible; (2) Strategy dominance's remaining gap (24.7% vs ≤20%) resisted every Director-lever combination tried without regressing Median Passage length/peak Line, which sit in the same knife-edge region. *Done when: give at least the `mixed` bot real Seal-telegraph dodging logic (read the active attack's safe position/gap the same way `press.test.ts`'s baseline bot already does, reuse for the harness), re-run the full sweep, and get both rows to PASS or documented as structurally as-designed with a second independent tuning attempt logged in DECISIONS.md.* — Gave every strategy (not just `mixed`) real dodging via `bot.ts`'s new `decideSealDodgeTargetX`; confirmed empirically (not just by prediction) that dodging structurally cannot move "First Seal reached" and that `seals.hpBase` is still not a working lever even at ~4x its value under this session's Director/economy state — both are the "second independent tuning attempt" this task asked for. Both rows stay FAIL, structurally as-designed for now; concrete next lever (decoupling Seal-attack damage from the shared Blot-contact constant) identified and logged, not yet attempted — see Task 7.9.

## Phase 5 — Ship

- [x] **5.1 PWA.** Manifest, hand-written service worker, generated icons, install prompt, offline verification test. *Done when: the built app loads and plays with the network disabled and Lighthouse PWA checks pass locally.* — Icons generated at build time by a from-scratch Node PNG encoder (no `canvas` package); service worker generated post-`vite build` from the real hashed asset list; install prompt wired to `beforeinstallprompt` with a permanent per-device dismiss; Wake Lock requested during a Passage. Verified offline play end-to-end via headless Chromium (title screen and real gameplay both render correctly with the network fully disabled, zero console errors). No actual Lighthouse run (not an approved devDependency) — manually verified everything its PWA category checks instead; logged in DECISIONS.md.
- [x] **5.2 Performance.** Adaptive quality manager, `npm run perf`, `PERF.md` with percentiles at max entity counts. *Done when: the §8 budget is met or the shortfall is documented with the measurement.* — `render/quality.ts`'s rolling-30-frame-average manager degrades stipple dot count, then trail repaint frequency, then density-block thresholds under sustained sub-52fps, and recovers under sustained comfortable fps; wired into `main.ts`'s render loop, never the fixed sim step. `npm run perf` stress-tests 400 Strokes/900 Blot in headless Node (simulation-only — no Canvas in Node without a forbidden devDependency) and writes `PERF.md`; a one-off headless-Chromium render-inclusive measurement is recorded there too. Real-device 60fps/cold-start/memory verification isn't possible in this environment — documented as the shortfall per this task's own acceptance bar, not claimed as a pass.
- [x] **5.3 Accessibility.** Shapes-Only toggle, greyscale legibility check, reduced-motion handling, focus states, hit targets ≥44px. *Done when: a greyscale screenshot still distinguishes all three classes.* — Shapes-Only stamps a small dot-count mark (1/2/3) on every Stroke and Slip; reduced-motion zeroes the ink trail's wobble/jitter (the mechanism Task 2.10 already flagged for this). Verified in headless Chromium: greyscale screenshot with both settings on still clearly distinguishes all three classes by silhouette alone. Explicit `:focus-visible` goldLeaf outline added globally; every interactive element confirmed ≥44px. GAME_DESIGN.md §12's camera-shake/hit-splat "motion rules" were never built by any earlier task (nothing to "halve" for reduced-motion) — logged as a gap, added as Task 7.10 rather than built unscoped here.
- [x] **5.4 Android.** Capacitor project, portrait lock, back-button handling, zero permissions, haptics, debug APK, `README.md` build docs. *Done when: the merged manifest declares no permissions and the APK path is documented.* — Full `/android` Capacitor project committed; `AndroidManifest.xml` declares zero `<uses-permission>` entries (removed Capacitor's default INTERNET request — this app never calls the network at runtime) and locks `android:screenOrientation="portrait"`. Back button mapped to Settings→Title / minimize-while-playing / exit-elsewhere via `@capacitor/app`, never a straight exit from a Passage. Haptics wired via `@capacitor/haptics` on Flourish (new `flourishTriggered` /sim event), Line loss, and Seal impact (diffed directly in `main.ts`, distinguishing the two by whether a Seal encounter was active). Launcher icons regenerated by the same from-scratch PNG encoder as the PWA icons (Task 5.1), replacing Capacitor's placeholder art — also fixed two real gaps in the generated scaffold along the way: a missing `colors.xml` (`styles.xml` referenced undefined `@color/colorPrimary` etc.) and the default white splash-screen PNGs, replaced with a plain colour matching the game's own background. Debug APK build genuinely attempted, not just documented — confirmed unbuildable in this session's own environment (no Android SDK, and its network policy blocks `dl.google.com`, the Android Gradle Plugin's Maven host, with a 403) — per TECH_SPEC.md §11's own explicit allowance for this case, the full project and build docs (`README.md`) are still committed, and the gap is logged honestly rather than claimed as verified.
- [ ] **5.5 Ship review.** `README.md` complete, `PLAYTEST_NOTES.md` with your own frame-level assessment of feel and at least five specific tuning observations, `DECISIONS.md` tidy, verify green, tag `v1.0.0`. *Done when: all of that is committed.*

---

## Phase 6 — Roguelite (LOCKED — begin only when told)

- [ ] **6.1** Stage/Scroll structure, fixed-length stages, five stage themes, Scroll progression and persistence.
- [ ] **6.2** Technique system: 24 run-scoped perks, the 1-of-3 between-stage choice screen, stacking rules, per-technique unit tests.
- [ ] **6.3** Four Brushes with starting compositions and passives; unlock conditions.
- [ ] **6.4** Relics: Seal rewards persisting within a Scroll.
- [ ] **6.5** Endless mode gated behind Scroll 8, sharing the Inkstone.
- [ ] **6.6** Balance pass for the roguelite layer with new harness targets; extend `BALANCE_REPORT.md`.
- [ ] **6.7** Mode select on the title screen; both modes share the Inkstone; ship review as 5.5.

## Phase 7 — Optional polish (pull from freely once 1–5 are done)

- [x] **7.1** Daily seed with a shareable result string (no network — copy to clipboard). — New `src/meta/dailySeed.ts`: `computeDailySeed` (FNV-1a hash of the UTC calendar date — same seed for every player on the same UTC day), `dailyDayNumber` (days since the Unix epoch, no arbitrary game-launch-date decision needed), `formatDailyShareText` (a compact `INKFALL — Day #N / <distance>u · Peak Line <n> · <n> Seals broken / <death cause>` block). Title screen gained a secondary "Today's Passage — Day #N" link (deliberately not a second primary button); the summary screen gained a "Copy result" row, visible only when the just-ended Passage used the daily seed, writing the share text via `navigator.clipboard.writeText` (a real user-gesture click, works offline — no network, no submission anywhere) and flashing "Copied" for 1.5s. A Daily Passage is explicitly excluded from the first-run scripted opening (GAME_DESIGN.md §13) even on a brand-new profile, so every player's "same day, same seed" result stays genuinely comparable. Fixed a real latent bug caught while wiring this: `beginPassage`'s new optional parameter meant its two existing bare-reference call sites (`onPlay: beginPassage`, and the old `onBegin: beginPassage`) would have received the click's `MouseEvent` as the seed argument — both now wrapped in `() => beginPassage()`. Verified end-to-end in headless Chromium: real UTC day number rendered, a forced death produced a summary whose clipboard write matched the exact expected format. `npm run verify` green (494 tests, build clean); never touches `/sim`, so no balance-harness re-run needed. Logged in DECISIONS.md.
- [x] **7.2** Replay from seed + input tape, playable back in-engine. — Recording is exactly the `WorldInput` `stepWorld` already consumes each fixed step, appended as it happens (`main.ts`); the loop's own existing determinism (fixed timestep, seeded RNG per concern, TECH_SPEC.md §4) makes seed + tape sufficient to reproduce a Passage bit-for-bit, proven by a new `tests/sim/world.test.ts` case that records a *varying* scripted input sequence, replays it against a fresh same-seed World, and asserts the two match exactly (plus a negative check that a different tape actually diverges, so the assertion isn't vacuous). Summary screen gained a "Watch replay" link, always available after any real Passage; a new `'replaying'` `AppState` reuses the exact same `stepAndReact` step/haptics/shake/music logic real play uses, fed from the recorded tape instead of live input, and never calls `handlePassageDeath` (a replay's result already happened for real the first time — re-recording it would double-count Gold Leaf/profile stats). Ends automatically (tape exhausted or `world.isDead`) back at Title; back-button during a replay also ends it cleanly rather than falling through to app-exit. In-memory only, scoped to the Passage that just ended — not persisted across reloads or saved as a library (that's closer to Task 7.3's territory). Verified end-to-end in headless Chromium: a real recorded run's death timing (~21s) reproduced almost exactly on replay, zero console errors, clean automatic return to Title. `npm run verify` green (495 tests, build clean); never touches `/sim` source (only its tests), so no balance-harness re-run needed. Logged in DECISIONS.md.
- [x] **7.3** Statistics screen driven by the rolling run history. — New pure `src/meta/statistics.ts` (`computeRunHistoryStats`) aggregates `Profile.runHistory` (TECH_SPEC.md §3's existing 40-run rolling window): median/average/window-best distance, median peak Line, chronological distances for a chart — `null` for an empty history rather than a table of zeroes. New `ui/screens/statistics.ts`, reached via a title-screen link (grouped with Daily/Settings as Title's other secondary destinations — Statistics is content to look at, not a mutable setting) shows lifetime totals (`Profile`'s own aggregate fields: total Passages, best distance, best peak Line, total Seals broken, lifetime Gold Leaf) plus the rolling-window stats and a plain DOM bar chart of recent distances (styled `div`s, no canvas — every other /ui screen is DOM-only, kept consistent). New `'statistics'` `AppState`; back-button treats it like Settings (returns to Title). Verified in headless Chromium: the empty-state message shows correctly on a fresh profile, and after two real deaths (persisted across an actual page reload) the screen shows correct totals, correctly-scaled chart bars, and correct aggregate numbers — zero console errors. `npm run verify` green (500 tests, build clean); never touches `/sim`, so no balance-harness re-run needed. Logged in DECISIONS.md.
- [x] **7.4** A photo mode that renders the ink trail of your best Passage as a downloadable PNG scroll. — New `render/scroll.ts` silently replays a Passage's seed + input tape (Task 7.2's `Replay` shape, extracted into shared `meta/replay.ts`) off-loop and paints the same width/colour wash `trail.ts` paints each frame (both now exported from there so the two can never drift), but onto one tall non-fading canvas indexed by distance travelled rather than real time — the whole Passage's trail unrolled start-to-death as a single image no live moment of actual play ever shows at once. Downloads via the browser's own native `canvas.toBlob('image/png')` (no custom encoder needed — unlike `build/generateIcons.ts`, a browser always has a real `CanvasRenderingContext2D`). "Best" is scoped to the current session (not persisted to `Profile` — a full input tape is real storage weight `Profile`'s schema doesn't need to take on for a Phase-7 polish task), tracked in `main.ts` alongside Task 7.2's `lastReplay`. Reached via a title-screen link, hidden until a session-best exists.
  Fixed a real, load-bearing gap discovered while wiring the title-screen entry point: Title has always been reachable "at the end of any Passage, not from mid-shop" (Inkstone's own original Task 4.3 comment) precisely because it held nothing worth revisiting mid-session — Daily/Statistics/Photo Mode (Tasks 7.1/7.3/7.4) changed that, and without any way back, all three become unreachable the instant a player leaves Title once, for the rest of the session. Added a secondary "Menu" link on the Inkstone screen (alongside the still-primary "Play," which keeps GAME_DESIGN.md §10's two-tap fast path exactly as fast as before) to fix this properly rather than leaving three new features half-reachable.
  Also fixed a real gap caught by actually downloading and opening a generated image, not trusting the math: the first working version used a fixed 2px-tall paint per replayed step, which left visible gaps once distance-per-step (forward speed ramps over a Passage) exceeded that fixed height at a legible pixel scale — replaced with a per-step slice height computed from the step's own actual distance delta, and the base scale itself raised from an initially-too-narrow 3px/u to 10px/u after the first real downloaded PNG came back nearly illegible. Verified end-to-end in headless Chromium across three real scenarios (debug-forced quick death, a debug-Line-size test that correctly exposed a pre-existing Task 7.2 limitation — debug hooks mutate `World` outside the recorded input tape, so replay/photo mode can never see them, same as real players never trigger them — and a fully natural undebugged playthrough): real PNG downloads every time, correct dimensions, continuous gap-free trail, zero console errors. `npm run verify` green (500 tests, build clean); never touches `/sim`, so no balance-harness re-run needed. Logged in DECISIONS.md.
- [ ] **7.5** Palette variants unlocked by milestones (must keep §12 legibility rules).
- [x] **7.6** Colour-blind simulation dev tool for verifying 5.3 automatically. — New `npm run check-colorblind` (`src/build/checkColorblind.ts`, Node-only, no browser/canvas dependency — same flat-pixel-buffer technique `generateIcons.ts` already uses) rasterizes the three Stroke glyphs' exact geometry, computes a colour-transform-independent silhouette (Jaccard) distance for every class pair, and separately reports fill-colour distance under greyscale + protanopia/deuteranopia/tritanopia simulation matrices for context. Writes `COLORBLIND_REPORT.md`; exits non-zero if any pair's silhouette distance drops below a floor. Refactored `render/strokes.ts`'s three separate glyph-drawing functions into one shared `strokeGlyphPolygon` (pure point geometry) + `drawGlyph`, verified byte-for-byte visually identical in headless Chromium before/after. `checkColorblind.ts` duplicates that same geometry rather than importing it (can't — it compiles under `tsconfig.cli.json`, which doesn't include /render), following the exact precedent `generateIcons.ts` already established for its own Harai-triangle copy; a first attempt at sharing the geometry via `/sim/stroke.ts` was reverted after `config.test.ts`'s "no stray magic numbers in /sim" check correctly caught it — cosmetic glyph geometry isn't gameplay balance data and doesn't belong behind that rule. New unit tests (`tests/build/checkColorblind.test.ts`) pin the three real glyphs' pairwise silhouette distances above the floor — a real regression catcher, not just a report generator. Logged in DECISIONS.md.
- [x] **7.7** Vendor the real fonts (TECH_SPEC.md §7): Shippori Mincho B1 and Zen Kaku Gothic New, subset to Latin+digits+punctuation, as `.woff2` under `/src/assets/fonts` with `OFL.txt` for each. Task 2.11 introduced the game's first on-screen text using a system font stack fallback (logged in DECISIONS.md) rather than blocking on font sourcing/subsetting tooling; this task replaces that fallback with the real faces everywhere text appears. — Sourced both families' official OFL release TTFs from the `google/fonts` GitHub repo (reachable through this environment's proxy even though `fonts.google.com` itself isn't), installed `fonttools`+`brotli` for this session only (a one-time dev-time step to produce a committed static asset, not a project devDependency) and subset each to Latin+digits+punctuation with `pyftsubset`, landing all four files (Regular + a bold weight per family — Shippori's ExtraBold, its heaviest, for GAME_DESIGN.md's "Gate numbers... at Shippori's heaviest weight" language; Zen Kaku's Bold for the UI weight-700 usages already in `widgets.ts`) comfortably under the 40KB-each target (34-37KB Shippori, 12KB Zen Kaku). `@font-face` declared in `index.html` (`font-display: block` for Shippori, `swap` for Zen Kaku, per TECH_SPEC.md §7 exactly); `ui/widgets.ts`'s `DISPLAY_FONT_STACK`/`UI_FONT_STACK` now lead with the real family names, keeping the old system stack as a legitimate fallback tail rather than deleting it. Verified in headless Chromium: `document.fonts` shows both real faces `loaded` (not the fallback), the title heading's computed `font-family` resolves to the vendored Shippori face, and offline reload after the service worker's initial precache still renders the real fonts with zero console errors — the built payload grew by ~95KB (fonts, already woff2-compressed) against a 600KB total budget, nowhere close. Discovered a real, separate, deeper gap while checking where GAME_DESIGN.md's "Gate numbers are the loudest type in the game" text is actually drawn: it isn't — `road.ts`'s `drawGatePair` renders each Gate half as a plain coloured quad with no number/text anywhere, DOM or canvas. Out of scope for a font-vendoring task; logged as new Task 7.11.
- [x] **7.8** Wire Gates' Temper track into the sim: `gates.ts`'s `TemperState` (rateStacks/rangeStacks/splashStacks/wetnessCapStacks, GAME_DESIGN.md §7.2's "Rate +20%, Range +25%, Splash +30%, Wetness cap +25") is accumulated on every Temper Gate resolution but never actually applied anywhere — discovered while wiring Task 4.2's Inkstone multipliers through the exact same call sites (fire rate, projectile range, Tome splash, Wetness cap) that Temper should also be composing into. The new multiplier parameters those call sites gained for Task 4.2 (`rateMultiplier`, `rangeMultiplier`, an eventual splash-radius multiplier, the Wetness cap override) are plain numbers a caller composes before passing in, so this should be a composition fix, not new plumbing. Logged in DECISIONS.md. — Added `computeTemperEffects` (`gates.ts`) and composed it alongside `world.upgradeEffects` at the same fire-rate/range/Wetness-cap call sites in `stepWorld`; added a new `splashMultiplier` param to `updateFiring`/`spawnProjectile` (`projectiles.ts`, appended last to avoid reshuffling an existing positional test call). New `tests/sim/temperEffects.test.ts` (4 tests) proves all four stats measurably change `stepWorld` behaviour, mirroring `worldUpgrades.test.ts`'s existing pattern. `npm run verify` green (456 tests); 2000-run harness re-run shows no §11 regression. Full record in DECISIONS.md.
- [x] **7.9** Give each Seal boss its own attack-damage number, decoupled from `BALANCE.line.normalContactStrokeLoss` (the shared ordinary-Blot-contact constant every boss's attack currently reuses) — discovered during Task 4.6b: even at ~4x `seals.hpBase`, Seal-caused deaths stayed at 0.0% because zero-upgrades DPS is never the bottleneck, and "First Seal broken" tracks "First Seal reached" almost exactly 1:1 (nobody who reaches a Seal loses the fight). A real, independently-tunable per-boss (or shared `seals.attackStrokeLoss`) hit cost is the untried lever that could make a dodged-but-imperfect fight genuinely lethal to a meaningful fraction of zero-upgrades runs, closing "First Seal broken" toward the middle of its 25-45% target band, without touching ordinary Blot-contact balance elsewhere. Logged in DECISIONS.md. — Split into three per-boss values (`seals.smear/press/blank.attackStrokeLoss`) rather than one shared number: Smear's own isolated zero-upgrades baseline test (a 3-Stroke starting Line, a dumb dodge-only bot) turned out to already be knife-edge at the old shared value (1) — raising it to 2 dropped that test from 25% to 0/40 wins with no §11 benefit, while Press's isolated test was completely unaffected by the same change (0 hits taken regardless) and Blank has no such test at all. Kept Smear at 1, moved Press and Blank to 4. Re-ran the full 2000-run harness: "First Seal broken" stays PASS (37.3%, unchanged), no other row regressed — same 9/11 PASS as before. Full record in DECISIONS.md.
- [x] **7.10** Build the camera-shake/hit-feedback system GAME_DESIGN.md §12's "motion rules" describe (camera shake on Seal impacts and Flourish only, ≤4px, ≤180ms; hit feedback is an ink splat plus a 60ms scale pop, never a flash) — discovered during Task 5.3 that no earlier task ever built this (no task in this file ever named it as a deliverable), so Task 5.3's reduced-motion "halve shake" clause currently has nothing to act on. `render/trail.ts`'s wobble is already wired to the `reducedMotion` setting (Task 5.3); once real camera shake exists, wire it to the same setting (halved amplitude, not disabled — §12 is explicit that reduced-motion halves shake rather than removing gameplay feedback). Logged in DECISIONS.md. — New `render/shake.ts` (`triggerShake`/`currentShakeOffset`, ≤4px/≤180ms decaying random-direction jitter, halved not disabled under reducedMotion) triggered from Flourish (its own event) and Seal impact (the same Stroke-loss diff `main.ts` already computes for haptics); applied as a translate around only the 3D-scene layers at `layers.compositeInto` (never the persisting road-trail canvas itself, and never HUD/vignette). Hit feedback (ink splat + 60ms scale pop) lives in `render/blot.ts` — a new `lastHitAtS` field on `Blot` (set by `collision.ts`) drives a pure, unit-tested `computeHitPop` the draw call reads; same fillStyle as the Blot itself throughout, no new colour. Verified no console errors under heavy combat (900-Blot debug wave) and Flourish in headless Chromium; the Blot pop/splat itself is visually subtle by design (Blot is deliberately "always the darkest thing on screen," GAME_DESIGN.md's own palette table) so correctness leans on the new pure-function unit tests (`tests/render/blot.test.ts`, `tests/render/shake.test.ts`) rather than screenshot pixel-hunting. `npm run verify` green (465 tests); 2000-run harness byte-identical to the pre-task report (purely cosmetic, no gameplay path reads the new field).
- [x] **7.11** Draw the actual number/effect on each Gate half — discovered during Task 7.7 that `render/road.ts`'s `drawGatePair` renders every Gate as a plain coloured quad with no text anywhere, DOM or canvas, despite GAME_DESIGN.md §12 explicitly describing "Gate numbers... the loudest type in the game — set them enormous, at Shippori's heaviest weight, with a bone stroke on the vermilion fill" and TECH_SPEC.md §7's `font-display: block` rule being justified specifically by "gate numbers must never render in a fallback." As it stands, a player currently cannot read what a Gate pair actually does (×2 vs -10, a conversion target, a Temper stat) before committing to a side — colour-family-coding (`gateColor`) is the only signal today. Needs real canvas text rendering (`ctx.font` referencing the now-vendored Shippori Mincho B1 ExtraBold face, loaded via `FontFace`/`document.fonts` before first use so canvas text doesn't silently fall back) sized/stroked per §12's description, showing each `GateEffect`'s actual value (the arithmetic op's number, the conversion target's class glyph, or the Temper stat's name) — Sealed stays exempt (§7.2: "shows only a seal mark," the one Gate that's supposed to hide its effect). Logged in DECISIONS.md. — New pure `gateEffectLabel` (`sim/gates.ts`) derives the exact §7.2 literal strings (`×2`, `→ Tome`, `Rate +20%`, ...) from `BALANCE.gates`'s own live values; `road.ts`'s `drawGateHalf` draws it in Shippori Mincho B1 ExtraBold (already vendored by 7.7 — no separate `FontFace` load needed, since the title screen's own `font-display: block` heading already guarantees it's loaded before any gameplay can start), vermilion fill with a bone stroke, sized "enormous" but auto-shrunk (measured via `ctx.measureText`, not guessed) to fit the door's own width so long labels never bleed into the neighbouring half. Sealed gates draw a small stamped ring-and-dot mark instead of any text. Verified in headless Chromium across many real spawned Gate pairs (debug hook): every family's label reads cleanly and never overlaps its neighbour, and a caught Sealed gate shows only the mark. `npm run verify` green (479 tests); 2000-run harness byte-identical to the pre-task report.
