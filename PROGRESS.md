# PROGRESS

Append one line per completed task. Newest at the bottom. This file is how a fresh
session recovers context after a usage limit — write it for a reader with no memory
of what you just did.

Format:
`- [YYYY-MM-DD] <task id> — <what landed> — <anything the next session must know>`

## Current state

- Phase: 5 (Phases 1-5 and Phase 7 all complete; Phase 6 explicitly locked)
- Next task: none in Phases 1-5/7 — everything through Task 5.5 and all of 7.1-7.11 is ticked. Phase 6 remains locked ("begin only when told"); nothing to resume until the user unlocks it or a new gap is discovered.
- Blockers: two structural §11 targets ("Strategy dominance", "First Seal reached") remain FAIL after three independent tuning attempts (Tasks 4.6/4.6b/7.9) — see DECISIONS.md's 2026-08-14 entry for why `v1.0.0` was tagged anyway and why this doesn't block anything further right now.

## Log

- [2026-08-07] 1.1 — Scaffolded Vite+TS strict project: tsconfig (app + a NodeNext `tsconfig.cli.json` for the headless `sim`/`perf` CLIs), ESLint flat config with a `Math.random` ban outside `/render`, Prettier, Vitest (node env, no jsdom dep), full `/src` folder skeleton per `TECH_SPEC.md` §3 with placeholder modules, `vercel.json` headers, minimal `manifest.webmanifest`, `README.md`. `npm run verify` green. — Next session: real per-file implementations start with 1.2 (fixed-timestep loop); CLI scripts import only `/core`, `/sim`, `/meta`, `/balance` and must use explicit `.js` extensions in relative imports for NodeNext resolution to work under `npm run sim`/`npm run perf`.
- [2026-08-07] 1.2 — `FixedStepLoop` in `src/core/loop.ts`: pure `advance(nowMs)` accumulator (no internal clock reads, so tests can feed fabricated timestamps), 60Hz fixed step, max 5 catch-up steps with remainder folded away on overrun, render alpha interpolation, pause/resume (resume re-primes `lastNowMs` so it doesn't burst-catch-up), `reset()`. `runInBrowser()` wraps it in `requestAnimationFrame`; `attachVisibilityAutoPause()` pauses on tab hidden; `mountDebugOverlay()` is a DOM-only fps/steps overlay gated behind `?debug=1` in `main.ts`. Determinism test drives 10,000 then 20,000 frames of exact fixed-dt length and asserts step count doesn't drift more than 1 step and the second identical-length segment matches the first — bit-exact equality against `N` isn't achievable with IEEE-754 timestamps at 1/60s, so the test asserts the property that actually matters (error doesn't compound). — Next session: `main.ts`'s `update`/`render` callbacks are still no-ops; they get wired to `/sim` and `/render` starting Task 2.1.
- [2026-08-07] 1.3 — `RngRegistry` in `src/core/rng.ts`: mulberry32 generator, one independent stream per concern (director/gates/slips/seals/cosmetic) each derived from the Passage seed via FNV-1a salting, plus `hashState()` (cyrb53 over a key-sorted canonical JSON stringify) for the determinism test. `Math.random` ESLint ban from 1.1 verified to actually fire outside `/render` (manually checked with a throwaway violating file). Determinism test drives a synthetic 5000-step RNG tape (standing in for the real World, which doesn't exist until Phase 2) and asserts byte-identical `hashState` at steps 100/1000/5000 for a replayed seed; a companion test confirms a cosmetic draw never perturbs another concern's sequence. — Next session: once `/sim/world.ts` lands (Task 2.x), point `hashState` at the real World snapshot for the full TECH_SPEC.md §12 determinism test instead of the synthetic tape.
- [2026-08-07] 1.4 — `Pool<T>` in `src/core/pool.ts`: fixed-capacity array pre-filled once by the factory, dense-array swap-remove for O(1) acquire/release with zero allocation after construction (non-null assertions scoped to this one file via an ESLint override, per TECH_SPEC.md §13's explicit carve-out for "pool internals"). `EventBus<Events>` in `src/core/events.ts`: a typed synchronous pub/sub for sim→render/audio signalling, documented as not allocation-safe if listeners (un)subscribe mid-emit — that's a deliberate constraint, not an oversight. Allocation test proves factory-call-count stays fixed across 600 acquire/release cycles and heap growth stays bounded; event bus has full pub/sub unit coverage. — Next session: 1.5 (viewport/input) is next; pools for the actual entity kinds (projectiles/blot/strokes/particles/floating numbers) get instantiated once `/sim` has real types to pool, starting Phase 2.
- [2026-08-07] 1.5 — `Viewport` (`src/platform/viewport.ts`): DPR-aware canvas backing store, letterboxed to the 9:21..3:4 aspect range against its container, safe-area insets read from CSS `env()` custom properties added in `index.html`. `InputSampler` (`src/platform/input.ts`): Pointer Events drag → world-unit `lateralDelta` (0.028 u/px) and keyboard A/D/arrows → 14 u/s, unified into one `InputFrame`; `holding` covers both pointer-down and a Space-key hold-to-Flourish binding (logged in DECISIONS.md — the spec requires a keyboard hold-to-Flourish key but doesn't name one). The critically-damped-follower math that turns `lateralDelta` into an actual Brush position lives in `src/sim/line.ts` (`stepCriticallyDamped`, exact closed-form solution, no overshoot on a step input) rather than in /platform, since it carries persistent state that must advance deterministically once per fixed step — /platform only ever hands the sim a raw per-step delta. `main.ts` wires Viewport+InputSampler+damped follower into a temporary debug marker (flat linear mapping, not the true projection) to prove the path end to end; verified in a real headless Chromium via Playwright (globally available in this environment, not added as a project dependency) — dragging the marker +200px moved it from x≈194.6 to x≈330.2 CSS px with smooth damping and no JS errors, fps/steps holding ~60. InputSampler is unit-tested against a hand-rolled fake EventTarget (no jsdom, per the §2 dependency list). — Next session: 1.6 is the BALANCE config object; the local constants in `main.ts`/`input.ts` (0.028, 14, clamp bounds, 0.07s tau) should migrate there and be imported instead.
- [2026-08-07] 1.6 — `BALANCE` in `src/sim/config.ts`: every gameplay-tunable number from GAME_DESIGN.md §3-11 (control, strokes, phrases, line/formation, wetness/flourish, road/slips/gates/sealstacks, blot/seals, spawn director, economy/inkstone, and the §11 balance targets for the harness to check against), grouped by section, `as const` + a recursive `deepFreeze()` so misuse is caught at both compile time (readonly literal types) and runtime (TypeError on mutation attempt). Percentages are stored as fractional multipliers throughout (0.04 not 4) so upgrade effects apply as `base * (1 + n * field)` with no stray `/100` anywhere. `main.ts` and `platform/input.ts` now import their lane/damping/control constants from `BALANCE` instead of local duplicates. Phase 6 numbers (Scrolls, Techniques, Brushes, Relics) are deliberately not encoded yet — nothing in the current codebase consumes them and Phase 6 is locked; they'll be added when Task 6.x actually starts. Added a test (`tests/sim/config.test.ts`) that mechanically enforces the "no magic numbers outside config.ts" rule: it scans every other file under `/sim` for bare numeric literals (exempting 0, 1, -1, and `[n]` array indices) — currently trivially green since Phase 2 hasn't added real `/sim` logic yet, but it'll catch regressions as soon as it does. **Phase 1 complete.** — Next session: Task 2.1 (projection + road) is the first real rendering work; `BALANCE.lane`/`forwardSpeed` feed directly into it, and the existing `main.ts` debug-marker wiring should be replaced by the real render pipeline rather than extended further.
- [2026-08-07] 2.1 — Implemented TECH_SPEC.md §5's projection: `render/camera.ts` holds the fixed camera constants and derives `cx/cy/unit/horizonOffsetPx` as fractions of the canvas box (not fixed pixel counts) so framing is identical at any letterboxed size — the two framing fractions (horizon position, world-to-pixel scale) aren't specified numerically anywhere, so I picked values that read as a natural racer-style vanishing point and logged the reasoning in code comments rather than DECISIONS.md (a rendering-feel choice, not a gameplay one). `render/projection.ts`'s `project()` is the literal formula; `DepthBucketList` is a zero-allocation counting-sort over pre-allocated typed arrays (64 buckets, far-to-near iteration) for the painter's-algorithm draw order Task 2.2+ will need for Strokes/Blot. `render/layers.ts` owns the 4 offscreen canvases (sky/water regenerated only on resize, road+trail redrawn every frame for now, actors cleared every frame, HUD stubbed) and composites them back-to-front plus a radial vignette onto the real canvas. `render/road.ts` draws the lane as a projected trapezoid with solid edge lines and a dashed centreline using a seamless modulo-phase scroll technique (`computeDashSpans`, pure and unit-tested for continuity across the phase wrap). Hit one real bug during visual verification: the initial near-draw-z (-4) put the road's near edge only ~68% down a 1920px-tall canvas instead of past the bottom, because camera pitch (camY=4.2) needs a depth close to NEAR_CLIP before the ground plane's projected edge clears the frame — fixed by moving NEAR_DRAW_Z to -5.8, verified visually before/after via a headless-Chromium screenshot (Playwright, globally available, not a project dependency). Verified in-browser at 390×844, 768×1024 (3:4), 400×933 (~9:21), and 1080×1920 — consistent framing, no seams, scroll rate confirmed via the debug overlay's steps/s. `main.ts`'s temporary Brush marker now renders through the real projection instead of a flat linear mapping. — Next session: 2.2 is the Brush/Line formation; it should consume `stepCriticallyDamped` from `src/sim/line.ts` (already built in 1.5) and draw through `DepthBucketList`+`project()` on the actors layer, replacing `main.ts`'s placeholder marker entirely.
- [2026-08-07] 2.2 — Formation math in `src/sim/line.ts`: `LineState` is an ordered array (front-most first, matching "newest fill from the back, losses remove from the front" directly — no separate index bookkeeping needed), `computeFormationSlot()` derives row/col/lateral/depth from a flat index, generalising GAME_DESIGN.md §5's "front row leads by 0.3u at centre" arc to every row (a parabolic bulge, documented as a deliberate generalisation beyond the literal spec). `classifyForRender()` implements the >60-Strokes density-block rule. Added `src/sim/stroke.ts` with just the `StrokeClass` type now, since formation needed it before Task 2.3 builds real class stats. Render side (`src/render/strokes.ts`): three distinct silhouettes (Hane tick, Tome block, Harai diagonal sliver) colour-coded per §4/§12, plus a density-block mass capped at a fixed max visual depth (3.5u) so a 400-Stroke Line can't project geometry behind the camera. Found and fixed a real bug via visual inspection: blending all three class colours by weight for the block's fill produced exactly the muddy tan GAME_DESIGN.md §12 explicitly forbids ("not a warm-cream-and-terracotta scheme") — switched to the dominant class's pure colour as the fill, with minority classes shown only as stipple dots (dots matching the dominant colour would be invisible anyway). Verified in headless Chromium at N=1/5/37/120/400 via debug keys 1-5 (gated behind `?debug=1`, GAME_DESIGN.md §13 non-goal on tutorial text doesn't apply to dev tooling): steady 60fps/60 steps-s even at N=400, correct arc/density-block transitions, no console errors. — Next session: 2.3 (Strokes and firing) adds the actual class stats table to `stroke.ts` and projectile pools; the debug round-robin class assignment in `main.ts`'s `buildDebugLine()` should stay as dev tooling, not be mistaken for the real Slip-driven composition system (Task 2.5).
- [2026-08-07] 2.3 — `src/sim/projectiles.ts`: a pooled `Projectile` type (`Pool<T>` capacity now sourced from a new `BALANCE.pools` section — TECH_SPEC.md §5's perf-budget numbers, not GAME_DESIGN.md's, but still centralized rather than left as literals). `computeFiringPlan()` implements §5's row-scaled DPS exactly: front row at full rate, back rows at `extraRowDpsContribution` (55%) each, capped at `maxVisibleProjectilesPerClassPerS` (24) with the surplus folded into a `damagePerProjectile` multiplier so total DPS is preserved regardless of cap. `updateFiring()`/`updateProjectileMotion()` mutate the pool in place by design (documented as a deliberate exception to /sim's usual copy-on-write style — projectiles are the one genuine up-to-2048-item-per-frame hot path). Found and fixed a real bug via unit tests before it ever hit the screen: `updateFiring`'s muzzle round-robin cursor was a local variable reset every call, so at typical fire rates (≤1 shot/frame) every projectile spawned from the same muzzle forever — moved the cursor into a persisted `ClassFiringState` alongside the time accumulator. Added `Pool.get(index)` for safe backward-iteration-with-release (needed so `updateProjectileMotion` can despawn expired projectiles without an intermediate array). `applyArmorMultiplier`/`registerHit` implement the §4 armour table and Harai's pierce-count-then-destroy lifecycle, fully unit-tested without needing real Blot/Slip entities yet (Task 2.4/2.5). Render side adds `drawProjectiles()` to `strokes.ts` (dot/blob/line per class) — not a new file, since /render's layout has no dedicated projectiles.ts. Verified in headless Chromium at N=400 with the debug Line: steady 60fps/60 steps-s while all three classes fire simultaneously, projectile counts visibly capped rather than flooding. — Next session: 2.4 (the Blot) is what projectile collision (`registerHit`, splash, armour) actually gets tested against for real; `computeRowClassCounts`/`computeFrontRowSourcePositions` in `line.ts` are the integration points Blot's spawn/contact logic will sit alongside.
- [2026-08-07] 2.4 — `src/sim/blot.ts`: all six Blot types (pooled, `BALANCE.pools.blotCapacity`=1200), `updateBlotMotion()` (straight march for four classes, Drifter lateral strafe+bounce, Blotter stop-at-range+periodic-lob), `resolveLineContact()` (Blot-reaches-Brush → 1 Stroke lost and dies, 3 for Crust, per §5 — fully unit-tested including a mixed-group-in-one-step sum check), `resolveBlotDeaths()` (hp≤0 cleanup + Splitter's 3-Smudges-at-±1u split, stress-tested with 100 simultaneous Splitter deaths to validate the backward-iteration-with-respawn invariant). Also built `src/sim/collision.ts` (projectile↔Blot hit detection, armour multiplier, Tome splash, Harai pierce) since Task 2.3 built the attacker half of combat and 2.4 is where a target first exists to test it against — not tied to its own task ID in TASKS.md but necessary for the combat loop to do anything. Two spec gaps logged in DECISIONS.md: Drifter's strafe speed (4 u/s) and the projectile-Blot hit radius (0.6u), neither given a number in GAME_DESIGN.md. `render/blot.ts`: a stable-per-pool-slot "ragged blob" silhouette (hash-seeded from `poolIndex`, not `Math.random()` per frame, so it doesn't flicker) for near/low-count Blot, cheap plain circles for the >150-count/>30u-distance mass tier (`BALANCE.blotRender`), matching §8.1's "resolves into individuals as it nears." Found and fixed a real pre-existing bug from Task 2.2 during this task's visual verification: the Stroke density-block's capped depth (3.5u) still let its near edge approach the projection's near-clip plane closely enough that its scale exploded, visually painting over the *entire* road instead of sitting near the Brush — tightened the cap to 1.8u. Verified in headless Chromium: 900 Blot + a 120-Stroke Line + full firing/collision/death pipeline running simultaneously holds 60fps, mass-to-individual transition and the corrected density block both read correctly. — Next session: 2.5 (Slips) is the first real source of Strokes joining the Line and Gold Leaf-less growth; it'll want `addStroke()` (line.ts, already built) and probably its own pooled entity type alongside collision.ts's pattern.
- [2026-08-08] 2.5 — `src/sim/slips.ts`: pooled Slip entity (+1/+5/+25 kinds, HP from BALANCE), `computeClassWeights()`/`pickSlipClass()` implementing §7.1's least-held-class-1.5×-more-likely rule via the seeded `'slips'` RNG concern (never perturbs other systems' sequences — exactly what that concern registry was built for in Task 1.3), and a `JoiningRecruit` pool for the runner-joins-the-back animation (travel duration logged as a spec gap in DECISIONS.md, 0.6s). `resolveSlipDeaths()`/`updateJoiningRecruits()` complete the pipeline: Slip hp≤0 → recruits spawn at its death position → travel to the Line's next formation slot → `addStroke()` on arrival. Extended `collision.ts` with `resolveProjectileSlipCollisions()` (same armour-multiplier pipeline as Blot, deliberately no Tome splash against Slips — splash is specified for Blot combat, and letting one shot clear a whole Slip run would undercut "shooting is the commitment"). Caught a real bug during visual verification, not by the unit tests (which spawn Slips already in range, so they didn't exercise this): Slips had no motion at all — GAME_DESIGN.md doesn't give them a speed stat since they're staked road furniture, not walkers, but that also means nothing was making them approach the Brush the way the road markings visually scroll. Added `updateSlipMotion()`, shifting z at `BALANCE.forwardSpeed.baseUPerS` (the same rate the world scrolls) since the Brush's own z is fixed at 0. Verified in headless Chromium: a debug-spawned Slip run scrolls into range, gets shot down, and the Line visibly grows from 3 to 5+ Strokes of the recruited class within a couple seconds, holding 60fps throughout. — Next session: 2.6 (Gates and Sealstacks) is next; Sealstack HP/impact will likely want the same "no stat given → needs its own scroll motion" check this task's bug came from, since it's also static road content.
- [2026-08-08] 2.6 — `src/sim/gates.ts`: `GateEffect` carries optional `arithmeticOp`/`conversionTarget`/`temperStat` fields (normally exactly one set; a Sealed reveal can set both arithmetic+conversion, which is the only place GAME_DESIGN.md §7.2's "multiply before add within a single gate; conversion applies after count changes" has any observable effect — decided and logged in DECISIONS.md, along with the pair-archetype mix). `generateGatePair()` guarantees the pairing rules *by construction* rather than rejection-sampling — three archetypes (20% Sealed-vs-modest, 30% safety-valve good-vs-bad, 50% explicit dilemma), each individually incapable of producing an illegal pair — verified with a 10,000-roll test asserting `isLegalPair` holds every time and the dilemma fraction clears the 0.25 floor (actual ~70%). `applyGateEffect()` enforces arithmetic-before-conversion in its own control flow, tested with a compound effect where getting the order wrong would leave newly-grown Strokes unconverted (a real, observable divergence, not just a call-order check). `src/sim/sealstacks.ts` mirrors the Blot/Slip pooled-entity pattern: `resolveSealstackContact()` implements the exact `ceil(remainingHP/12)` impact formula, dodging to the unblocked lane half costs nothing. Extended `collision.ts` with `resolveProjectileSealstackCollisions()` (a lane-half + z-thickness hit test, not circle-vs-circle, since Sealstacks are wide). Logged two more spec gaps (Sealstack HP=60, hit-thickness=1.0u) in DECISIONS.md. Render additions went into `road.ts` (family-colour-coded gate halves, a dark Sealstack block) since neither fit the render layout's existing files better; `CLASS_COLOR` was exported from `strokes.ts` so road.ts could reuse it for Conversion gates instead of duplicating the palette mapping. Verified in headless Chromium: a debug-spawned gate pair and Sealstack both render, resolve on contact, and visibly affect the Line (this session's Line started at only N=3, so a bad-arithmetic or Sealstack outcome zeroing it to empty is correct behaviour, not a bug), holding 60fps throughout. — Next session: 2.7 (Spawn Director + balance harness) is next and is a big one — it's what finally decides *when* Blot waves, Slip runs, and Gate pairs actually appear during a Passage, replacing every debug-key spawn built across 2.2-2.6 with real pressure-driven spawning; it also stands up the headless harness and bot strategies from TECH_SPEC.md §6, which nothing so far has needed.
- [2026-08-08] 2.7 — `src/sim/director.ts`: pure Spawn Director functions matching §9's formulas exactly (`computePressure`, `computeWaveIntervalS`/`computeWaveSize`, the pressure-band `computeComposition` table with anti-snowball's Crust bump and the mercy rule's Crust/Splitter suppression, `computeSlipBudgetPer100U`, `planSlipSpawn` for turning a HP budget into a concrete +1/+5/+25 run, and the full mercy-rule state machine). `src/sim/world.ts`: the `World`/`stepWorld` consolidation Task 2.7 is really about — ties every Phase-2 subsystem (Line, projectiles, Blot, Slips/recruits, Gates/Temper, Sealstacks) into one steppable root state, with the Director driving real wave/Slip-run/Gate-pair/Sealstack spawning on time/distance thresholds instead of the debug-key spawns Tasks 2.2-2.6 used. `computeGoldLeaf()` implements §10's formula (Seals term always 0 — Seals don't exist until Phase 3).
  Two real bugs found while writing `world.test.ts`, both fixed:
  1. **Not a bug, but looked like one**: firing has no auto-aim by design (confirmed via GAME_DESIGN.md §8.1's Drifter — "forcing you to lead your shots" only makes sense if the *player* aims, via Brush position). A stationary centred Brush only lines up with 1-in-`rowSize` muzzles, so two tests expecting near-instant kills on a target planted at a fixed x were wrong, not the sim — fixed the tests to give targets realistic travel distance instead. Logged in DECISIONS.md.
  2. **A real pool leak**: unshot Slips never left `slipPool` (only `resolveSlipDeaths` released them, nothing released a *missed* one). `slipCapacity` (64) is smaller than what a multi-minute Passage offers (~14 Slips every ~100u), so any run that ignores Slips exhausts the pool partway through and spawning silently stops thereafter — added `resolveMissedSlips()` (slips.ts) releasing anything `missDespawnMarginU` (2u) past the Brush, wired into `stepWorld`, regression-tested (a 6000-step unshot run now stays bounded instead of leaking).
  `src/balance/bot.ts`: all five TECH_SPEC.md §6 strategies as pure `World → WorldInput` functions — `greedy-slips`/`greedy-kill` do what they say (nearest Slip vs. a sliding-window densest-Blot-cluster search), `mixed` switches to kill-mode when projected 2s Blot arrivals exceed `N × 0.4`, `gates` adds an expected-value comparison between an inbound Gate pair's two sides (Sealed evaluated by its known 60/40 reveal odds, never the concrete rolled effect — a real player can't see through the seal either), `random` is a smooth seeded wander standing in for a player not really looking. `src/balance/harness.ts`: the CLI (`--runs`/`--strategy`/`--upgrades`/`--seed`/`--sweep`/`--out`), running Passages to death (or a 400s safety cap, recorded as `timeout`) and measuring death-within-8s-of-Gate itself since World doesn't track it. `src/balance/report.ts`: `BALANCE_REPORT.md` generator — ASCII histograms, death-cause breakdown, a §11 pass/fail table that honestly reports N/A (not a fabricated verdict) for every target whose prerequisite doesn't exist yet (Seals, upgrades, per-Blot-class death attribution).
  Also fixed `tsconfig.cli.json` (`npm run build:cli` had never actually been run before — not covered by `npm run verify` — and failed immediately on `core/loop.ts`'s DOM types; added `"dom"` to `lib`, type-only, the CLI never calls the DOM-touching methods). `--sweep` parses but doesn't yet override `BALANCE` (it's deliberately a frozen singleton per Task 1.6, and real sweeping needs a config-injection path threaded through `createWorld`/`stepWorld` — that's Task 4.6's job). Ran `npm run sim -- --runs 2000`: completes in ~11 minutes, writes `BALANCE_REPORT.md`. Targets all FAIL/N/A as expected at this stage — Passage length and peak Line both blow past target ranges (median ~389s vs. 55-85s target, peak Line pinned at the 999 hard cap for the `mixed` bot), meaning the Director's pressure ramp is nowhere near tuned yet; that tuning is explicitly Task 4.6's job, not this one's. — Next session: **known tech debt** — `main.ts` still has its own inline simulation wiring from Tasks 2.2-2.6's debug spawns, separate from `world.ts`'s consolidated version; they're not yet unified, so `main.ts`'s Director-adjacent behavior (spawn positions etc.) doesn't match `world.ts`'s real Director logic. Refactoring `main.ts` to consume `World`/`stepWorld` directly is unstarted and should happen before or during Task 2.8 gets much further, or the two will keep drifting apart. 2.8 (Wetness and Flourish) is next.
- [2026-08-08] (tech debt, ahead of 2.8) — Refactored `main.ts` to consume `sim/world.ts`'s `World`/`stepWorld` directly instead of its own inline copy of the same simulation wiring (predating `world.ts`, left over from Tasks 2.2-2.6's incremental builds). `update()` is now just "sample input, call `stepWorld` once"; `render()` reads straight from `world.*` (blotPool, slipPool, currentGatePair, line, distanceU for road scroll, etc.) instead of a dozen separate local variables. All debug keys (1-9, 0, G, H) retarget the same `World`'s pools/line; added `KeyR` to spawn a fresh `World` (previously there was no way to recover from an empty Line — there's still no real death/restart screen, that's Task 2.11, but the debug build isn't stuck anymore either). Verified in headless Chromium at 390×844 with `?debug=1`: steady 60fps/60 steps-s, no console errors, and — the actual point of this refactor — Blot waves now appear on their own within ~15s of load without touching a debug key, which is the real Spawn Director finally driving ordinary play instead of only reacting to manual spawns. Doing this now rather than after 2.8 avoids building Wetness/Flourish's `stepWorld` integration twice.
- [2026-08-08] 2.8 — `src/sim/wetness.ts`: `WetnessState` (drains 6.0/s flat while firing regardless of N, refills 25/s once 0.8s have passed since firing last stopped, both exactly per §6) plus a pooled `InkPool` entity (staked road furniture like Slips/Sealstacks, restores 40 Wetness and self-releases the instant the Brush crosses it, hit or miss). `src/sim/flourish.ts`: an edge-triggered charge/release state machine (`stepFlourishInput` — a hold's validity, insufficient Wetness or still on cooldown, is decided once at the moment it starts and never changes mid-hold, so `isPulsing` can't silently flip into a real charge) plus `applyFlourishSweep`'s `28*(1+0.15*rowCount)` damage in a 5u forward arc with 4u knockback, no armour multiplier (the formula has no StrokeClass to look one up against). Both wired into `world.ts`: holding now genuinely suspends the normal auto-fire ("Wetness converts the always-firing default into an occasional decision to stop"), a triggered sweep's damage lands before that same step's `resolveBlotDeaths` so a Flourish kill counts exactly like any other, and the dry state (`isWetnessDry`) halves fire rate via a new optional `rateMultiplier` param threaded through `projectiles.ts`'s `computeFiringPlan`/`updateFiring` (defaults to 1, so every existing call site is untouched).
  Two spec gaps logged in DECISIONS.md (ink pool hit radius 1.2u and lateral spawn range, cooldown-during-hold treated the same as insufficient-Wetness) plus one deliberate no-op: Flourish's "deletes all Blot projectiles on screen" clause has nothing to delete yet, since Blotter's ink lob is a direct instant hit, not a pooled in-flight entity — flagged in `flourish.ts`'s docblock so it isn't forgotten if that changes.
  Render: `render/strokes.ts`'s new `drawBrush()` replaces the flat placeholder circle main.ts drew since Task 1.5 — Wetness renders as a bottom-up "liquid fill" inside the Brush's own body (not a bar, per §6) inside a faint always-visible ring so an empty Brush still reads as present; at Wetness 0 the empty fill and a computed desaturation (blended from the one declared `bone` palette colour, not a new hardcoded swatch) double up to make the dry state unmistakable. Charging a valid Flourish grows the Brush and tints it vermilion with charge progress; an invalid hold pulses alpha instead. Verified in headless Chromium: screenshotted full vs. dry (after ~18s of continuous auto-fire, confirming the drain math), and pixel-sampled the charging Brush mid-hold to confirm the colour actually hits `#D33A2C` (vermilion) rather than eyeballing a small on-screen circle.
  Found a real regression while running the mandatory `npm run sim -- --runs 200` gameplay check (CLAUDE.md workflow step 4): none of the five bot strategies (built in Task 2.7, predating this task's economy) ever stop firing, so every bot run hit 0 Wetness within ~17s and then spent the *entire rest of the Passage* at half fire rate — badly skewing every balance number this task's own harness produces. Fixed with a shared, strategy-agnostic "rest" instinct in `bot.ts` (hold once Wetness ≤10, resume once it climbs back to ≥60, hysteresis so it doesn't flicker at the boundary) rather than touching any strategy's actual targeting logic. Regenerated the committed `BALANCE_REPORT.md` from `npm run sim -- --runs 2000` afterward — still all FAIL/N-A as expected (Task 4.6's job, not this one's), numbers moved but nothing suggests a new crash/hang/instant-death-loop regression.
  30 new unit tests (`tests/sim/wetness.test.ts`, `tests/sim/flourish.test.ts`) plus 7 new `world.ts` integration cases covering the full stepWorld wiring (holding suspends drain, a full charge-and-release costs Wetness and starts the cooldown, releasing early does nothing, dry state measurably halves projectile spawn count, ink pool contact restores Wetness). — Next session: 2.9 (Phrases) is next; note that Flourish's Blot-projectile-deletion clause and any bot logic that deliberately *chooses* to Flourish for damage (rather than just resting to avoid the dry penalty) are both still open, non-blocking gaps.
- [2026-08-08] 2.9 — `src/sim/phrases.ts`: front-row detection (`detectPhraseEligibility`, a pure function of one class's front-row count, reusing `line.ts`'s existing `computeRowClassCounts` rather than a new formation query — worth noting for anyone re-deriving this: 2 classes can never be simultaneously eligible, since the 5-slot front row can't fit two 3+ groups) and three independent per-class cycle timers (matching `projectiles.ts`'s per-class-accumulator shape), each resetting to zero progress (no partial credit) the instant its class drops below the 3-Stroke threshold. Effects: Hane's Scatter (`applyHaneScatter`) hits the nearest up to 9 Blot in a 45° forward cone; Tome's Press (`applyTomePress`) damages and staggers everything in a 3u×18u corridor in one hit-test rather than a genuinely travelling shockwave (no travel speed is given in GAME_DESIGN.md, logged in DECISIONS.md); Harai's Sweep is the one Phrase modelled as actually persisting — a `SweepState` accumulator ticks `sweepDamagePerTick` at `sweepTicksPerS` against everything ahead of the Brush for the full `sweepDurationS`, with its 5-of-a-kind damage multiplier captured once at the triggering fire so it can't retroactively change if the front row shifts mid-sweep. None of the three apply an armour multiplier — like Flourish, they're a flat Phrase amount, not a Stroke's own shot.
  `blot.ts` gained a real (if small) addition: `staggeredUntilS`, a sim-time deadline `updateBlotMotion` now checks first thing (`isBlotStaggered`) — a staggered Blot skips its whole turn (march, strafe, lob-timer) until it passes, defaulting via an added `timeS = 0` parameter so every pre-existing call site (tests included) is unaffected. `config.ts` gained one non-BALANCE export, `DEG_TO_RAD`, purely so the `scatterSpreadDeg`→radians conversion in phrases.ts doesn't need a bare `180` literal, which the magic-number scanner (`config.test.ts`, exempts only 0/1/-1/2/-2) would otherwise reject outside config.ts.
  Render: `render/effects.ts` (previously a stub) now draws all three — Hane a fan of short jade rays fading in 0.15s, Tome a single vermilion corridor pulse fading in 0.2s, Harai a sustained translucent bone-coloured full-lane wash for the actual duration `isSweepActive` reports true — deliberately three different shapes/motions, not three colours of the same flash, per §12's silhouette-first rule. `strokes.ts`'s `Quad` type and `fillQuad` helper were exported so effects.ts didn't need to duplicate quad-filling (same reuse pattern as `CLASS_COLOR` in Task 2.6). Added debug keys J/K/L (pure 5-Hane/5-Tome/5-Harai Lines) since the existing debug Line builder round-robins classes and could never actually trigger a Phrase — a genuinely useful standing dev tool, not a throwaway. Verified in headless Chromium: spawned a Blot wave, forced each pure Line in turn, and screenshotted through each ~3s cycle boundary — the three effects are unmistakably different shapes at a glance (radiating lines vs. a solid block vs. a full-width wash), matching the acceptance bar exactly.
  45 new tests (`tests/sim/phrases.test.ts`), including the 30-hand-written-formation table the acceptance criterion asks for (covering every class at every relevant count boundary — 0, 1, threshold-1/threshold, rowSize-1/rowSize — plus back-row-never-counts and arrival-order-shuffle cases), plus cycle-timing, per-effect hit-test, and sustained-sweep coverage. Ran the mandatory `npm run sim -- --runs 200` (CLAUDE.md workflow step 4): numbers stayed in the same broad (still-unbalanced, expected) range as Task 2.8's baseline, no crash/hang — regenerated the committed `BALANCE_REPORT.md` from a fresh `--runs 2000` pass regardless, per TECH_SPEC.md §6's "regenerate after every tuning change." — Next session: 2.10 (the ink trail) is next, explicitly flagged in TASKS.md as worth spending real effort on.
- [2026-08-08] 2.10 — The ink trail (GAME_DESIGN.md §12), pure `/render` work — no `/sim` changes, so the mandatory gameplay-regression `npm run sim` check doesn't apply this task. Reworked the road+trail layer from "fully cleared and redrawn every frame" (its Task 2.1 state) into the genuine accumulation layer TECH_SPEC.md §5 always specified: `layers.ts` gained a `needsRoadTrailReset` flag (same regen-on-resize pattern as sky/water); `road.ts` split into `resetRoadTrailBase` (paints the opaque lane once, on load/resize) and a per-frame `drawRoad` that no longer clears — it fades the whole layer by `TRAIL_FADE_ALPHA` (6%, TECH_SPEC's own number) toward the lane's base colour, then redraws the edge lines and this frame's dashes opaque on top (edges: fade-then-redraw-identically keeps them permanently crisp; dashes: since their screen position shifts slightly frame to frame from the scroll-phase trick, this leaves a very minor multi-frame ghost trail on them too, judged acceptable/on-theme rather than a bug — nothing else about "no shimmer or seam" from Task 2.1 is affected).
  `render/trail.ts` (previously a stub): `drawInkTrail` needs no memory of past Brush positions at all — it just paints the Line's *current* footprint (width `sqrt(N)`-scaled and capped, not literally linear, since a linear width at N=999 would blow past the lane itself and GAME_DESIGN.md §12 only says "proportional," not a formula; colour class-mix-blended per §12, unlike the density block's deliberately-unblended fill, because a translucent repeatedly-faded wash reads as genuinely mixed ink rather than the flat muddy colour that specifically ruled out blending for solid Stroke silhouettes) onto the layer every single frame; the layer's own non-clearing fade is what turns repeated painting into a persisting, decaying trail. This is also what makes "persists ~4s... a huge Line lays down a river of ink; a dying Line leaves a thin scratch" true for free, with no persistence timer anywhere: a wide trail overlaps itself more as the Brush drifts, so it takes more 6%-decay cycles to fade below visibility than a thin one does — the width *is* the persistence. Capillary wobble/bleeding edges: an independent sine wobble plus `Math.random()` jitter (render cosmetics are exempt from the seeded-PRNG rule) on each edge, structured as one clearly separable constant pair so Task 5.3's "disable trail wobble under `prefers-reduced-motion`" can zero it later without restructuring anything.
  Verified in headless Chromium: with the Brush held stationary, a debug N=5 Line and an N=400 Line produce a clearly, measurably different trail width (screenshotted side by side); weaving the Brush left/right for ~2.5s produces a wavy, bleeding-edged accumulated stroke, not a straight bar; a stress pass (N=400 Line + a 900-Blot wave, full trail/Phrase/Flourish rendering all active at once) held a steady 60fps/60 steps-s with no console errors.
  Correction to my own note above: GAME_DESIGN.md §7.4's "also visually reset the ink trail the Line leaves behind" is about driving through an **Ink pool** (§6/Task 2.8's Wetness pickup), not death — I misattributed it while writing this entry. It's still unimplemented (an ink-pool hit currently has no distinct trail-side visual at all, only the Wetness-state change main.ts's `drawBrush` already shows), but it's a small, self-contained polish gap, not something blocking 2.10 or 2.11's acceptance criteria — logged here so it isn't lost. — Next session: 2.11 (Death and restart) is next.
- [2026-08-08] 2.11 — **Phase 2 complete.** `world.ts` gained `peakLineCount` (tracked on World itself now, not just locally inside the harness's run loop — `harness.ts` was updated to read it instead of duplicating the tracking, one source of truth). The death sequence is driven by *real* elapsed time, not `world.timeS`: `stepWorld` returns before advancing `timeS` once `isDead`, so the World genuinely freezes the instant it dies (a correct freeze-frame falls out of the existing architecture for free — nothing new needed there), and `main.ts` tracks its own `deathAtRealMs` (via `core/time.ts`'s `nowMs()`) to drive everything that has to keep moving after that: `render/trail.ts`'s new `drawInkBleed` (a growing dark Blot-coloured puddle, distinct from the ordinary class-blended trail — the Line's just been reduced to 0, so there's no class mix left to blend) and `render/hud.ts` (new, previously a stub) drawing a death-cause line, one animated number (Gold Leaf earned — GAME_DESIGN.md §10 says exactly one number animates, doesn't say which; picking the reward figure over e.g. distance was my call), then the rest of the run's real stats (distance, peak Line, Blot unbound, Seals broken — genuinely 0, not a placeholder, since Seals don't exist yet) as static text, all fading in together. "Best-ever comparison" is the one §10 stat *not* shown — there's no persistence yet (Task 4.1) to compare against, and a comparison row with nothing behind it would be exactly the kind of half-implemented element CLAUDE.md's quality bar rules out, so it's omitted outright rather than faked.
  This is also the first on-screen text anywhere in the game, which meant confronting font vendoring earlier than expected (TECH_SPEC.md §7 wants two vendored OFL `.woff2` subsets) — used a system font stack instead (serif for the animated number, sans for labels) rather than spending the turns to source/subset/verify real font files mid-task, logged in DECISIONS.md with a new TASKS.md entry (7.7) to swap the real faces in later, everywhere text ends up appearing.
  Restart: any pointer tap (always) or keypress (desktop, disabled under `?debug=1` so it doesn't fight debug-key testing) restarts instantly once dead — no confirmation, and never blocked by the reveal animation still playing, so an impatient tap works immediately. `layers.ts` gained `requestRoadTrailReset()` so restart also visually clears the old Passage's ink/death-bleed (reusing the accumulation-reset mechanism Task 2.10 built for window resize) rather than the new Passage inheriting old ink. Since Inkstone (Task 4.2) doesn't exist yet, the loop is simply Death → summary (auto) → tap → new Passage — one tap, not the full spec's eventual two, which only makes the "under 3s" bound easier to clear, not harder; Task 4.2/4.3 will insert the Inkstone stop later without changing this.
  Verified in headless Chromium: forced a 1-Stroke Line into a Blot wave, confirmed the freeze/bleed/summary sequence renders correctly with real computed stats (screenshotted mid-reveal), tapped to restart, and confirmed a fresh Passage begins with the old trail/summary completely gone. Ran the mandatory `npm run sim -- --runs 200` (CLAUDE.md workflow step 4, since `world.ts`/`harness.ts` changed): numbers came back byte-identical to Task 2.9's baseline at the same seed, confirming `peakLineCount` tracking is purely observational — regenerated the committed `BALANCE_REPORT.md` from a fresh `--runs 2000` pass regardless. — Next session: **Phase 3 (The Seals) starts with Task 3.1**, the first genuinely new system since Phase 1 (boss framework: phased HP, telegraphs, stagger windows) — nothing in Phase 2 built any of this, so it's a from-scratch module, not an extension of existing `/sim` code.
- [2026-08-08] 3.1 — `src/sim/seals/framework.ts` (new subdirectory, per TECH_SPEC.md §3's file layout): everything a Seal fight shares regardless of which boss is in it. HP is one continuous pool (not `phaseCount` separate pools — "segmented HP bar (segments = phase count)" read to me as a display convention, logged in DECISIONS.md), so damage during the ×2 stagger window can carry straight through a segment boundary instead of being wasted, the standard "punish a stagger" shape. `phaseIndex` (which attack pattern is active) only formally advances once a stagger window fully elapses — never mid-stagger, even if the pool has already crossed further boundaries, which keeps `createBossState` (a fresh attack state per phase) from ever running mid-window. The generic `startAttack`/`stepAttack` pair is the "telegraph before resolve" shape GAME_DESIGN.md §8.2 requires of every attack, every boss: `startAttack` clamps below-minimum requests up to `minAttackTelegraphS` unconditionally, so no boss (this one or a future one) can construct an under-telegraphed attack through the framework at all — a structural guarantee, not a convention to remember.
  `src/sim/seals/stub.ts`: the minimal boss Task 3.1's acceptance criterion needs — 3 phases (borrowed from `BALANCE.seals.smear.phases` rather than inventing an unlisted balance number for a test fixture), one attack ("Pulse": telegraph a lane half, cost a Stroke if the Brush is still there when it resolves — §8.2's "a correct answer that is a position, not a reflex"). `stubActiveTelegraph` is a typed accessor into its own opaque `bossState` for render, the same pattern `phrases.ts`'s `lastFiredAtS` already established.
  World integration: `world.seal`/`sealDefinition` (null when no encounter), a new `DeathCause` value `'seal'`, and `world.sealsBroken` (finally giving `computeGoldLeaf`'s `sealsBroken` parameter — hardcoded 0 everywhere since Task 2.7 — something real to read; updated `main.ts`/`harness.ts`/`hud.ts`'s call sites). A Seal holds at a fixed `engagementZU` (16u, a new logged decision — no stand-off distance is named in GAME_DESIGN.md) once fighting, closing there smoothly from the normal wave-spawn distance across the 6s approach so "a vertical seal-mark rises in the distance" has somewhere to rise from (`computeSealZ`, shared by world.ts's own collision test and render). Blot-wave/Gate/Sealstack Director spawning pauses while an encounter is in progress; Slip runs don't (§8.2 calls this out explicitly). `startSealEncounter` is a direct hook (main.ts's new `KeyB` debug key), not automatic Director cadence — that's explicitly Task 3.5's job ("75s cadence in arcade"), so nothing here tries to guess it. Reordering the Director's per-step trigger checks (to gate three of them behind `seal === null` while leaving Slip spawning unconditional) shifted the shared `'director'` RNG concern's draw order slightly whenever a wave/Gate/Sealstack and a Slip run become due on the same step — harmless (still fully deterministic per seed, just a different valid sequence than before), but it's why three of five bot strategies' numbers moved slightly in this session's `BALANCE_REPORT.md` regen despite no gameplay logic changing for them (bots never trigger a Seal encounter, so the new code path itself never executes in harness runs).
  Render: `render/seal.ts` (new) — deliberately minimal, proving the framework's own mechanics are visible rather than attempting real boss art (Tasks 3.2-3.4's job): a dark monolith silhouette (vermilion and outlined while staggered, so "hit it now" reads at a glance), a screen-space segmented HP bar (world-projecting it would shrink it unreadable at exactly the range that matters), and — via `stubActiveTelegraph` — a flashing vermilion half-lane warning during the stub's telegraph. During approach, only a thin rising vermilion sliver shows (§8.2's seal-mark), no HP bar or telegraph yet (nothing is fightable until the approach ends).
  19 new tests (`tests/sim/seals.test.ts`): the HP curve formula, full approach→fighting→phase-advance→broken lifecycle, `applySealDamage`'s stagger/double-damage/immediate-break rules, the shared telegraph shape (clamped minimum, never resolves early, resolves exactly once), and — the acceptance criterion itself — a full World-integration test that fights the stub through all 3 phases confirming `sealsBroken` increments and every `phaseIndex` was actually observed, plus a dedicated test proving dodging every telegraphed attack (by steering to the opposite half) costs zero Strokes while standing still through one costs exactly one. Verified in headless Chromium: screenshotted the rising approach mark, the segmented HP bar + dark body + a live vermilion telegraph flash all together mid-fight, and a Passage actually ending with deathCause `'seal'` correctly surfacing as "Broken by the Seal" on the Task 2.11 summary screen. Ran the mandatory `npm run sim -- --runs 200` (CLAUDE.md workflow step 4): no crashes, no `seal` deaths in bot-driven runs (expected — nothing auto-triggers a fight yet), numbers shifted slightly for the RNG-ordering reason above but stayed in the same broad range; regenerated the committed `BALANCE_REPORT.md` from a fresh `--runs 2000` pass. — Next session: 3.2 (The Smear) is next — the first *real* boss, plugging into this session's framework as a `SealDefinition` exactly the way the stub does, with its own `bossState` shape for the lateral-sweep/gather-pull/residue attack pattern.
- [2026-08-08] 3.2 — `src/sim/seals/smear.ts`: The Smear, the first real boss, plugging into Task 3.1's framework exactly like the stub does. Mid-implementation caught and fixed a real design mistake (worth reading DECISIONS.md for): my first reading of "sweeps an arm laterally across two thirds of the lane" made the *arm itself* two-thirds of the lane wide, which leaves only a one-third gap that itself keeps moving — that's a reflex check, not the "correct answer that is a position, not a reflex" GAME_DESIGN.md §8.2 explicitly requires. A test trying to assert a stationary safe spot exists caught this by failing the geometry. Fixed by splitting the number into two: `sweepReachFraction` (2/3 of the lane) is the sweep's *total footprint* across its whole duration, and a new, narrow `armWidthU` (2u) is what actually moves through that footprint — this leaves a stationary position near either lane edge safe for the attack's entire duration, a real "pick a spot and hold it" answer. Attack shape: `gatherPullS` (0.9s, named exactly) telegraph → the arm sweeps for `sweepDurationS` (1.2s, not named, chosen slower than its own telegraph) → cooldown (`attackIntervalS`, 2.5s) or, phase 3 only, a `residuePhase3S` (2s, named exactly) hazard left at the sweep's end position. The residue doesn't run through `startAttack`/`stepAttack` itself — it's a continuation of the already-telegraphed sweep ("trailing"), not a fresh attack needing its own warning. Both the sweep and the residue can only land a Stroke loss once per instance (a `hasHit*` flag), not once per frame of contact.
  Render: `render/seal.ts` gained a `definitionId`-based dispatch (`drawBossAttackVisual`) so it can draw either the stub's or the Smear's attack without either boss needing to know about the other; the Smear's own visual (`drawSmearVisual`) reads three genuinely different shapes across its three stages (a thin gathering sliver at the sweep's start position during telegraph, the arm's real current width and position while sweeping, a static fading stain at the end position during residue) rather than one shape recoloured three times.
  9 new tests (`tests/sim/smear.test.ts`), including the geometry check that caught the arm-width bug, the full attack-cycle state machine, hit/no-hit detection, a full World-integration fight through all 3 phases while actively dodging (returning to lane centre between attacks to actually damage the boss — firing still has no auto-aim, so camping the safe edge forever would mean never landing a hit either), and Task 3.2's own acceptance-adjacent baseline: since the Inkstone/upgrade system doesn't exist until Task 4.2, "the bot beats it at ~40% with level-5 upgrades" can't be literally measured yet (logged in DECISIONS.md) — measured a zero-upgrades baseline instead (67.5% over 40 seeds with a simple return-to-centre-or-dodge-to-edge bot) and reported it honestly as a starting point for Task 4.6, not asserted as a pass/fail bar. Verified in headless Chromium: the rising approach mark, segmented HP bar, dark body, and the sweeping vermilion arm telegraph all render correctly together, and a weak (N=1) Line standing still is correctly killed by the sweep with deathCause `'seal'` surfacing as expected on the summary screen. Ran the mandatory `npm run sim -- --runs 200`: numbers came back byte-identical to Task 3.1's baseline (bots never trigger a Seal encounter, so this task's code never executes in harness runs) — `BALANCE_REPORT.md` needed no regeneration. — Next session: 3.3 (The Press) is next — rhythmic slam/ring-gap attacks, with its own acceptance bar (ring gaps always reachable from any starting lateral position, asserted over 5,000 seeds) that's a more rigorous version of the same "positional answer" property this task's own geometry test checked more loosely.

- [2026-08-08] 3.3 — `src/sim/seals/press.ts`: The Press, plugging into Task 3.1's framework the same way the Smear does. Reinterpreted GAME_DESIGN.md §8.2's "a shockwave ring; you must be outside the ring or in the one gap in it" for the game's lateral-only movement: with no radial dimension, "outside the ring" collapses to "outside the whole lane except one gap window," and the slam resolves instantly (same instant-resolve precedent as Tome Phrase Press and the Smear's residue — nothing here has a specified travel speed). Phase 1 (index 0) is one gap; phase 2 (index 1) adds a second, offset ring whose gap centre is rolled within `gapHalfWidthU` of the first's — **by construction, not rejection sampling** (Task 2.6's Gate-pairing idiom), guaranteeing the two gap intervals always share a real, positive-width safe interval. Phase 3 (index 2) doesn't stack a third ring — it's a 2-beat rhythm, two independently-telegraphed single-ring slams separated by a short `beatGapS` pause (reasoning for treating phase 3 as "harder in time, not in space" is in DECISIONS.md). Every slam resolve spawns `summonPerSlamCount` (8) Smudges via the new `SealStepContext.blotPool`, evenly spread across the lane at `BALANCE.seals.engagementZU` — this is the first boss to touch the Blot pool, which required extending the shared `SealStepContext` (and `world.ts`'s `sealCtx` construction) with a `blotPool` field; `tests/sim/seals.test.ts` and `tests/sim/smear.test.ts` needed their `makeCtx` helpers updated for the new required field, a mechanical fix with no behavior change.
  Render: `render/seal.ts` gained a `'press'` branch (`drawPressVisual`) — each active ring draws as the lane *minus its own gap*, two flanking translucent quads either side of the gap window; phase 2's two overlapping rings compound into visibly darker vermilion everywhere except their shared safe gap purely from alpha blending, no special-casing needed. `main.ts` gained a `KeyN` debug hook (`?debug=1`) alongside the existing `KeyB`/`KeyM`.
  10 new tests (`tests/sim/press.test.ts`), including Task 3.3's literal acceptance criterion — the dual-ring gap overlap guarantee, verified over **5,000 seeds**, asserting a real positive-width shared-safe interval exists and sits within the Brush's reachable clamp range every single time. Also: the single-ring attack cycle, the minimum-telegraph floor, exact Smudge-summon count on resolve, hit/no-hit detection at the gap boundary, the full 2-beat phase-3 sequence, and a full-World termination test. That last one caught a real bug during development: the first draft's integration-test loop watched only `world.seal !== null`, not `world.isDead` — since `stepWorld` becomes a full no-op once the Line dies (a documented existing behavior, not new), a losing run span the whole 200,000-step budget doing nothing instead of ending; fixed by checking `!world.isDead` in the loop condition, same class of bug Task 3.2's own dodge tests hit and fixed. Unlike the Smear, a perfect ring dodge alone doesn't guarantee a Press win: the Smudge summons are a real independent Blot-contact threat, and Slip runs (which keep recruiting during the fight, §8.2) sit on a lane verge a dodge-only bot never visits — logged in DECISIONS.md. The zero-upgrades baseline test's bot now triages (dodge > intercept the nearest closing Smudge > collect the nearest Slip > hold centre and attack) rather than reusing the Smear's simpler bot; with that triage it wins 80% over 40 seeds (an honest, non-blocking measurement, same spirit as the Smear's 67.5%).
  Verified in headless Chromium: screenshotted the two-gap slam telegraph (two vermilion zones flanking a clear centre gap) and the 8-Smudge summon spread evenly around the boss immediately after a slam resolves — both render as designed. Ran the mandatory `npm run sim -- --runs 200`: bots never trigger a Seal encounter (still true — Task 3.5 is what wires Seals into normal Director-driven play), so this task's code never executes in harness runs; the regenerated 200-run report differed from the committed 2000-run one purely from sample-count noise, so `BALANCE_REPORT.md` was left untouched (reverted) rather than committing a lower-fidelity report for a no-op change, matching Task 3.2's own precedent. — Next session: 3.4 (The Blank) is next — four phases, road erasure, Stroke-to-Blot conversion; the one Seal explicitly noted as compounding when losing, and the first to need render changes to the road/trail layers themselves, not just its own attack visual.
- [2026-08-08] 3.4 — `src/sim/seals/blank.ts`: The Blank, the third and final real boss, four phases. Two attacks alternate on one shared cadence (never simultaneous — reasoning in DECISIONS.md): the **beam** (GAME_DESIGN.md §8.2's "removes Slips and Gates from the road for 3s") targets the road/economy, not the Brush, so it has no positional dodge — it resolves to a new optional `growthEraseS` field on `SealBossStepResult` (framework.ts), a pure instruction; `world.ts` is the one that actually clears `world.slipPool` and `world.currentGatePair` and sets a new `growthErasedUntilS` field that the existing Slip-spawn trigger now checks alongside its distance threshold, exactly the "just re-checks next time" shape the wave/Gate/Sealstack pause already used from Task 3.1. The **cone** *is* a position — a single `coneHalfWidthU`-wide danger band, instant-resolve (same precedent as the Press's ring) — and a Stroke it catches isn't just lost, it's *converted*: spawned back as an attacking `'smudge'` Blot a short distance ahead of the Brush via `ctx.blotPool` (the Press's summon idiom, reused as-is), which is the actual mechanism behind "the only Seal where losing feels like it compounds."
  Render: `render/seal.ts` gained a `'blank'` branch that draws the two attacks as deliberately different kinds of warning — the beam flashes the *whole* lane in bone (an erasure, not a danger zone, since there's nowhere to dodge to), the cone draws a single vermilion band at a fixed position, same shape as the Smear's arm/Press's gap. Phase 4's "erases the road markings entirely, leaving only the ink trail to navigate by" is render-only: a new exported `isRoadMarkingsErased(seal)` (pure function of `SealEncounterState`) feeds a new `eraseMarkings` param on `road.ts`'s `drawRoad`, which skips only the edge lines/centre dashes — `drawInkTrail` is called completely separately in `main.ts` and was never touched, so the trail structurally can't be gated behind the same flag; this is also exactly what Task 3.4's acceptance criterion ("erasure never leaves the player with zero navigable information") is checking for. `main.ts` gained a `KeyV` debug hook alongside `KeyB`/`KeyM`/`KeyN`.
  16 new tests: `tests/render/seal.test.ts` (new file) unit-tests `isRoadMarkingsErased` directly against hand-built `SealEncounterState` fixtures — true only for the Blank's own final phase while actually fighting, false for every other phase/status/boss. `tests/sim/blank.test.ts` covers the full beam-then-cone attack cycle, both telegraphs' minimum-length floor, the cone's exact convert-on-hit behaviour (one Stroke in, one Blot out, verified both at a guaranteed miss and at the actual rolled cone centre), and a full-World integration test proving the beam really does clear an active Slip run and Gate pair and holds off a Slip run that becomes "due" mid-erasure until the window actually clears. Verified in headless Chromium: screenshotted the beam's full-lane bone flash, the cone's vermilion band, the 4-segment HP bar, and the stagger tint on a fresh phase transition — all render as designed; didn't chase a live phase-4 screenshot (a real fight through three stagger windows is slow to drive by hand) since the render logic is simple enough that the dedicated `isRoadMarkingsErased` unit tests plus a read of `drawRoad`'s early return cover it directly.
  Ran the mandatory `npm run sim -- --runs 200`: bots still never trigger a Seal encounter (Task 3.5 is what wires Seals into normal Director-driven play), so nothing in this task's code executes in harness runs; the regenerated 200-run report differed from the committed 2000-run one purely from sample-count noise, so `BALANCE_REPORT.md` was left untouched, same as Tasks 3.2 and 3.3. **All three Seals now exist.** — Next session: 3.5 (Seal scaling) is next, and it's a different kind of task from 3.1-3.4 — no new boss, just wiring what already exists into the real 75s arcade cadence (`startSealEncounter` has only ever been a debug hook so far) and confirming the index-based HP curve/cycling order hold up under the harness at scale.
- [2026-08-08] 3.5 — Wired Seals into real Director-driven play: `world.ts` gained `maybeStartSealEncounter`, called every step once the Director section runs. It's a fixed 75s (`arcadeCadenceS`) metronome scheduled from when the *previous* encounter started, not when it ended (same "just re-checks next time" shape every other Director timer already uses, logged in DECISIONS.md), cycling through `BALANCE.seals.order` and reusing `world.sealsBroken` directly as both the cycle position and the HP-growth `sealIndex` (§8.2's formula is already index-driven, so one counter does both jobs). `startSealEncounter` is no longer only a debug hook — `main.ts`'s `KeyB`/`KeyM`/`KeyN`/`KeyV` keys and every existing per-boss test still call it directly and are unaffected.
  This surfaced a real cross-task interaction: `tests/sim/smear.test.ts`/`press.test.ts`/`blank.test.ts`'s own long-running "full integration"/win-rate loops could now have the arcade cadence auto-start a *second*, different-boss encounter partway through (real Passage behaviour, but not what those boss-specific tests mean to measure) — fixed by setting `world.nextSealAtTimeS = Infinity` right after each of their manual `startSealEncounter` calls, restoring test isolation without touching any boss logic.
  `src/balance/report.ts`/`harness.ts`: `ReportedDeathCause` gained `'seal'` (it always existed on `world.deathCause`, just wasn't in the harness's narrower type), and `RunResult` gained `sealReached`/`sealBroken` (derived from `world.sealsBroken` and `world.seal !== null` — the latter freezes non-null the instant a run dies mid-encounter, per `stepWorld`'s own early return, so a run that died mid-fight without breaking one still counts as "reached"). `BALANCE_REPORT.md`'s two Seal §11 rows flip from N/A to real PASS/FAIL for the first time.
  New tests: `tests/sim/sealCadence.test.ts` (4 tests, using the harness's own `mixed` bot purely to survive long enough to reach the 75s mark — a stationary Brush dies to ordinary Blot attrition well before then) verifying no encounter starts early, one starts automatically right on schedule with no debug hook, the cycle order/`sealIndex`/HP curve are all correct across a full smear→press→blank→smear loop, and a long fight never gets silently swapped to the next boss mid-encounter even past a second threshold.
  Ran `npm run sim -- --runs 200`: "First Seal reached, zero upgrades" now PASSes (95.5% vs ≥70% target) — the cadence itself is confirmed working at the expected rate. "First Seal broken, zero upgrades" FAILs, but high (95.5% vs a 25–45% target band) with 0% Seal-caused deaths — not a cadence bug: none of the five harness bot strategies have any Seal-attack dodging logic, and by ~75s their Lines have typically already grown near the pool cap, giving DPS that clears a 420 HP boss in a swing or two. That's Task 4.6's balance pass to fix, not this one's (logged in DECISIONS.md). A follow-up `--runs 2000` pass was started for a higher-fidelity committed report but ran long (>15 minutes, unusually slow for the fresh Seal-fight simulation load) and was killed rather than block the task further; `BALANCE_REPORT.md` is committed from the 200-run pass instead — still a real, honestly-generated report, just a smaller sample. **Phase 3 (The Seals) complete.** — Next session: Phase 4 (Meta and shell) starts with 4.1 (Profile and persistence) — a different kind of task again: storage schema/migrations rather than sim/render work, and the first task whose acceptance bar explicitly requires correctness under a *failing* storage backend.
- [2026-08-08] 4.1 — First Phase-4 task, and the first task in this whole project that isn't `/sim` or `/render`: `core/save.ts` (generic `runMigrations`/`safeJsonParse`, deliberately knowing nothing about what a Profile contains, so a future Phase-6 save shape can reuse it), `platform/storage.ts` (`KeyValueStorage`, real localStorage picked only after an actual write-then-remove probe succeeds — not just a `typeof` check, since private-mode Safari can throw on the property getter itself — falling back to an in-memory store otherwise; every method independently try/catch-wrapped so a backend that passes the probe but throws later, e.g. quota exceeded, still degrades gracefully rather than crashing), and `meta/profile.ts` (the v1 `Profile` schema: Gold Leaf, the 8 Inkstone track levels, bests, totals, a reserved-but-unused `settings` object, and a 40-run rolling history — TECH_SPEC.md §3's full itemized list except Phase-6 fields, deliberately omitted since Phase 6 is locked and adding an empty placeholder now would defeat the whole point of having a migration chain; reasoning for both calls in DECISIONS.md).
  `loadProfile`/`saveProfile`/`exportProfile`/`importProfile` never throw — a malformed JSON blob, an unrecognized `schemaVersion`, or the storage backend itself throwing on every call all fall back to a fresh default profile rather than blocking the game, satisfying the task's own acceptance bar at both the storage layer (`platform/storage.ts`) and the profile layer independently (a caller could hand `loadProfile` a raw, unwrapped throwing backend directly, not just one that went through `createStorage`'s own fallback). `sanitizeProfile` defends against a *partially* corrupt profile field-by-field (one bad number from a hand-edited import falls back to that field's default, not the whole profile) rather than an all-or-nothing reset. `recordRunResult` folds one finished Passage into a Profile — bests only ever increase, Gold Leaf and totals always accumulate, the rolling history caps at 40 by dropping the oldest first — pure, with saving left to the caller.
  The migration chain itself (`MIGRATIONS` in `profile.ts`) is currently empty, since no schema below v1 has ever shipped — it's still fully wired through `loadProfile`/`importProfile` (not deferred) so a real future migration is a one-line addition, and the chain *mechanism* (`runMigrations` in `core/save.ts`) is proven correct against a synthetic "v0 -> v1" fixture in `tests/core/save.test.ts`, honestly labelled as a test-only invention rather than real product history (logged in DECISIONS.md).
  None of this is wired into the actual game loop yet — no screen calls `loadProfile`/`saveProfile` during real play, since there's no natural integration point without Task 4.3's screens (title/summary/Inkstone) or Task 4.2's upgrade *effects*. This task built and fully tested the standalone module, the same shape Task 3.1's Seal framework took before Task 3.2 plugged a real boss into it. 41 new tests across `tests/core/save.test.ts`, `tests/platform/storage.test.ts`, and `tests/meta/profile.test.ts` (new `tests/meta/` directory). Didn't touch `/sim` gameplay logic, so the mandatory `npm run sim -- --runs 200` regression check was skipped per CLAUDE.md's own "for any task touching gameplay" scope. — Next session: 4.2 (Economy) is next — `meta/economy.ts`/`upgrades.ts` (currently placeholder stubs) implement the Gold Leaf formula and apply all eight Inkstone tracks' *effects* into the sim (not just store their levels, which this task already did); it'll be the first thing that actually calls `loadProfile`/`recordRunResult` for something real.
- [2026-08-08] 4.2 — `sim/upgradeEffects.ts` (new): `computeUpgradeEffects(levels)` translates raw Inkstone levels into the actual multipliers/bonuses `stepWorld` consumes — computed once per Passage into a new `World.upgradeEffects` field, never by mutating `BALANCE` (still a deeply frozen singleton). Every one of the 7 gameplay-facing tracks (Leaf is economy-only, see below) got wired into its real call site, following the `rateMultiplier` idiom `wetness.ts`'s dry-fire state already established: Opening Stroke (`createWorld`'s starting `createLine` count), Grind/Nib/Reach (`computeFiringPlan`/`updateFiring`/`spawnProjectile` in projectiles.ts gained `damageMultiplier`/extended `rateMultiplier`/`rangeMultiplier` params), Reach's Slip-damage half (`applyArmorMultiplier`/`resolveProjectileSlipCollisions` gained a `slipDamageMultiplier` param), Well (`wetness.ts`'s `createWetnessState`/`stepWetness`/`resolveInkPoolContact` all gained a `cap` param), Flourish Study (`stepFlourishInput` gained a `cooldownS` param, pre-floored by `computeUpgradeEffects`), and Second Draft (a new `World.revivesRemaining`, seeded from how many of the {1,4,8} level thresholds are met and decremented by a rewritten `killLineIfEmpty`, which now refills the Line to `revivePeakFraction` of its peak — with Hane, logged in DECISIONS.md — instead of ending the Passage, whenever a revive is still available).
  `InkstoneTrackId`/`INKSTONE_TRACK_IDS` moved from `meta/profile.ts` (Task 4.1) into `sim/config.ts` — `/sim` needed the same shape and can't depend on `/meta`, so the canonical definition now lives in `/sim`, with `profile.ts` importing it instead of keeping its own copy. `meta/economy.ts`'s `computeGoldLeaf` (moved here from `world.ts`, per that function's own long-standing comment anticipating exactly this) gained a `leafLevel` parameter for the Leaf track — the one track with no `/sim` footprint at all, since it only ever affects the post-run total. `meta/upgrades.ts` gained `INKSTONE_TRACKS` (display metadata, every description string derived from `BALANCE.inkstone` directly so it can't drift from the real numbers), `computeUpgradeCost` (the `round(base × 1.38^L)` curve), and `purchaseUpgrade` (the actual spend-and-level-up transaction, pure).
  A discovered, pre-existing, out-of-scope gap: Gates' Temper track (`gates.ts`'s `TemperState`) has been accumulating rate/range/splash/Wetness-cap stacks since Task 2.6 but never actually applying them anywhere — found while wiring these exact same call sites for Inkstone. Not this task's job to fix (Gates, not Inkstone); logged in DECISIONS.md and added as Task 7.8, since the new multiplier parameters compose cleanly with whatever that eventual fix needs.
  49 new tests: `tests/sim/upgradeEffects.test.ts` (pure formula checks per track, including the {1,4,8}-threshold-counting for Second Draft and the cooldown floor for Flourish Study), `tests/sim/worldUpgrades.test.ts` (the actual "not just stored" requirement — full `createWorld`+`stepWorld` runs proving each track measurably changes real simulation output: more Blot/Slip damage, more projectiles fired, a higher Wetness cap that's both started at and refilled to, a longer projectile range, a shorter Flourish cooldown, and a real revive-instead-of-death with the Line correctly refilled and the budget correctly exhausted after use), `tests/meta/economy.test.ts`, and `tests/meta/upgrades.test.ts`. Two of the worldUpgrades tests needed real debugging, not just first-draft passes: Grind's "Blot loses more HP" comparison initially used a window either too short (no shots had traveled the 45u spawn distance yet, so *zero* damage landed in either case) or too long (the target died and its pool slot got silently reused by later Director activity, making a re-queried `hp` meaningless) — fixed by holding a direct object reference from `spawnBlot`'s own return value (never re-querying the pool by index) and empirically timing a window that's long enough for the first hits to land but short of the target's actual death, verified against a quick throwaway debug script.
  The harness's `--upgrades N` flag (parsed since Task 2.7 but never wired to anything) now applies level N uniformly across all eight tracks for that batch — a blunt "evenly-bought player" stand-in, not a real spend pattern, adequate for this task's job (proving effects apply) and explicitly left for Task 4.6 to replace with something realistic. `BALANCE_REPORT.md`'s two "all upgrades level 5" §11 rows flip from N/A to real PASS/FAIL when run with `--upgrades 5`; the zero-upgrades rows now correctly go N/A instead of silently mismeasuring when a batch isn't actually zero-upgrades. Ran `npm run sim -- --runs 200` twice: at the default `--upgrades 0`, every number came back byte-identical to the last committed report (confirming `NO_UPGRADES`'s all-zero levels are a true no-op — no regression), and a second `--upgrades 5` pass separately confirmed the new wiring produces materially different, sensible numbers (documented in the commit). — Next session: 4.3 (Screens) is next — title/run-summary/Inkstone/settings, the first task that actually wires `meta/profile.ts` and this task's `meta/upgrades.ts`/`economy.ts` into the live game loop, since nothing has called `loadProfile`/`purchaseUpgrade`/`recordRunResult` from `main.ts` yet.
- [2026-08-08] 4.3 — All four `/ui/screens` (title, summary, inkstone, settings) built and wired into a real top-level state machine in `main.ts` (`AppState` from the new `ui/screens/run.ts`: `title | playing | summary | inkstone | settings`). Screens are plain DOM overlays over the game canvas, not another Canvas 2D layer — logged in DECISIONS.md; real `<button>`/`<input>`/`<textarea>` elements give keyboard nav and the Inkstone's native scrolling for free. `ui/widgets.ts` gained the shared builders (`createScreenOverlay`, `createButton`, `createTrackRow` — the whole 10-mark filled/unfilled row *is* the buy button, `disabled` when maxed/unaffordable makes "inert" a real native state, not a style), `ui/strings.ts` gained every user-facing string in the game so far.
  `main.ts` now owns the full loop `loadProfile` → Title → `beginPassage` (`createWorld(seed, profile.upgradeLevels)`, finally wiring Task 4.2's upgrade effects into real play, not just tests) → death → `recordRunResult`+`saveProfile` → Summary (translucent, over the still-animating frozen/ink-bleeding canvas from Task 2.11 — verified in headless Chromium) → tap Continue → Inkstone (`purchaseUpgrade` live, no confirmation) → tap Play → a fresh Passage. That's exactly GAME_DESIGN.md §10's "two taps and under 3 seconds" from death back into play, and it's now for real — Task 2.11's own restart-on-any-tap shortcut is gone, replaced by this actual flow. Settings (mute/Shapes-Only/reduced-motion toggles, last-Passage seed, profile export/import) is reachable only from Title — no in-run pause mechanic exists anywhere in GAME_DESIGN.md, so inventing a second way in would be scope the task doesn't ask for (logged in DECISIONS.md). The three toggles persist for real (verified surviving a page reload in headless Chromium) but have no consumer yet — Tasks 4.4/5.3 build what they'd gate; this is the same "reserve the shape, wire the effect later" pattern `meta/profile.ts` already used.
  `render/hud.ts` lost `drawDeathSummary` and everything around it (the DOM summary screen is strictly more capable — real best-ever comparison via Profile, keyboard nav — and GAME_DESIGN.md only ever specified one summary presentation) but keeps `inkBleedFraction`, since the ink-bleed canvas animation still runs underneath the new translucent panel.
  No new Vitest coverage — this project has no jsdom (TECH_SPEC.md §2's dependency list doesn't include one, same reasoning Task 1.5's `InputSampler` test already established for `/platform`), so DOM-heavy `/ui` screens aren't unit-testable here; verified instead via headless Chromium click-throughs (screenshotted every screen, drove the full title→playing→death→summary→inkstone→playing-again loop end to end, and separately verified Settings' toggles/export/import including a real page reload to confirm persistence). Caught and fixed one real bug this way: the Inkstone's gold-leaf line was rendering as a label+value pair through `createStatRow`, showing "0 Gold Leaf12 Gold Leaf" (the static label text and the live value both said "N Gold Leaf") — simplified to a single line. Also caught that Settings' live export blob didn't refresh after toggling a setting mid-view (only on next screen-open) — fixed by refreshing the settings screen after every `onSettingChange`, not just after import.
  Existing typecheck/lint/427-ish Vitest suite all still pass unchanged. Ran the mandatory `npm run sim -- --runs 200`: byte-identical to the last committed report — this task never touches `/sim`, so the harness (which drives World directly, never `/main.ts` or `/ui`) couldn't have been affected either way. — Next session: 4.4 (Audio) is next — WebAudio synthesis for every SFX plus the generative music, the mixer, and the mute-persistence Settings' toggle from this task is finally ready to actually gate.
- [2026-08-08] 4.4 — `src/audio/synth.ts`: the shared WebAudio primitives (`getMixer` — one lazily-created AudioContext + sfx/music GainNode buses, `resumeAudioContext` for the gesture-unlock requirement, `playNoiseBurst`/`playTone`/`startDrone`). A single shared 2s noise buffer is generated once at mixer creation (seeded via `RngRegistry`'s `'cosmetic'` concern — `/audio` joins `/render`'s `Math.random` eslint carve-out, logged in DECISIONS.md), never per-SFX-call, keeping every actual trigger allocation-free and non-blocking (CLAUDE.md's "audio never blocks the loop").
  `src/sim/events.ts` (new): `GameEvents`, the exact four events GAME_DESIGN.md §12 names sounds for (fire/recruit/gateResolved/sealApproach) — no more, deliberately (logged in DECISIONS.md). First real use of Task 1.4's `EventBus`. `World` gained an `events` field (a fresh bus per Passage, same lifetime as `rng`); `projectiles.ts`'s `updateFiring` gained an optional trailing `events` param (existing test/harness call sites all still compile unchanged); `world.ts` itself emits `recruit` (right where a Slip's `addStroke` already lands), `gateResolved` (right where a Gate pair resolves), and `sealApproach` (from `startSealEncounter`) — no other `/sim` file needed touching. `/sim` still never imports `/audio` — it only ever emits into a bus /audio happens to be listening on.
  `src/audio/sfx.ts`: `attachSfx(events)` maps all four onto synth.ts — Hane a high bandpass tick, Tome a low lowpassed thud, Harai a bandpass burst whose filter cutoff actually sweeps mid-burst ("brushed"), Recruitment two soft sine notes a perfect fifth apart, Gate a highpass-to-lowpass swept burst ("paper tear"), Seal a detuned two-oscillator drone. Fire is throttled per class in the audio layer (not at the `/sim` emission site — DECISIONS.md) so a 400-Stroke Line's continuous fire reads as "a lot of fire," not overlapping noise. The Seal drone is a bounded ~6s cue tied to `BALANCE.seals.approachTelegraphS`, not a persistent sustain needing its own end-event (also logged).
  `src/audio/music.ts`: a two-voice generative pattern over a minor pentatonic scale (root/m3/P4/P5/m7 — chosen over major for mood, logged), tempo interpolated between a 2.6s (slow, low Pressure) and 0.9s (tight, high Pressure) note interval as `computePressure` climbs, on its own `setInterval`-driven lookahead scheduler (never a note fired synchronously off the render/sim loop — every note's actual start time is scheduled via WebAudio's own clock through `playTone`'s new `startDelayS` option). `setSealNear(true)` fades the two voices out and crossfades in a single low drone on the music bus; `setSealNear(false)` resumes both voices cleanly from "now" rather than bursting through a backlog of missed notes.
  `main.ts`: creates the mixer once at bootstrap and applies the loaded Profile's `muted` setting immediately; a one-time document-level `pointerdown`/`keydown` listener resumes the AudioContext on the player's very first interaction, whichever it is; `attachSfx` is re-subscribed to each fresh World's own event bus inside `beginPassage`; the update loop feeds `computePressure`/`world.seal !== null` into the music controller every step; Settings' mute toggle and profile import both call `mixer.setMuted` immediately, actually completing Task 4.3's "persisted but unconsumed" mute toggle.
  4 new Vitest cases (`tests/sim/projectiles.test.ts`, `tests/sim/world.test.ts`) proving the `/sim`-side event emission directly: `fire` events match real spawn count/class exactly and omitting `events` never throws (every pre-existing call site), `recruit` fires with the joined class, `gateResolved` fires exactly once per resolution, `sealApproach` carries the right `sealIndex`. No `/audio`-side unit tests — this project has no jsdom/DOM audio-graph test environment (same reasoning Task 4.3 already established for `/ui`); verified instead in headless Chromium by instrumenting `AudioContext.prototype.createOscillator`/`createBufferSource`/`resume` and confirming real node counts climb correctly through a full play session (fire, a Slip recruit, a debug Gate, and a Seal approach), with zero console/page errors and the context resuming exactly once on the first real gesture.
  Ran the mandatory `npm run sim -- --runs 200`: byte-identical to the last committed report — event emission is purely additive (no return values or RNG consumption changed), so harness-driven gameplay outcomes are provably unaffected. **Phase 4's Task 4.1-4.4 all now land** — no audio file anywhere in the repo, every §12 SFX exists and is distinct, mute genuinely persists and applies live. — Next session: 4.5 (First-run teaching) is next — GAME_DESIGN.md §13's scripted opening 20 seconds for a fresh profile, with the hard constraint that no text panel appears; then 4.6 (the real balance pass) closes out Phase 4.
- [2026-08-08] 4.5 — First-run teaching, GAME_DESIGN.md §13: "the first 20 seconds of a first-ever Passage present exactly one Slip run, then one Gate pair, then one wave, with nothing else on screen." Implemented entirely inside `/sim`: `World` gained a `firstRunTeaching` field (`null` for every ordinary Passage — `createWorld`'s new third parameter defaults `false`, so every existing caller/test/harness run is unaffected) driving a small stage machine (`slip -> gate -> wave -> settling -> done`) stepped once per frame in `stepWorld`. Each stage spawns through the *same* `spawnSlipRun`/`generateGatePair`/`spawnWave` the normal Director already uses — "scripted" only ever means "on a fixed clock instead of the usual one," never different content — and the normal wave/Gate/Sealstack/Slip/ink-pool triggers are all suppressed for the window's duration via one added `!teaching` clause per site (the same shape Task 3.1 already used to pause Director spawning during a Seal fight).
  Caught a real bug while testing, not just in a first draft: re-arming each suppressed schedule immediately as its own scripted stage fired left a ~5s trailing gap (wave fires at 15s, window closes at 20s) in which the Slip-budget schedule in particular could legitimately cross its threshold again — a second Slip run technically still inside "the first 20 seconds." Fixed with a 'settling' stage: the wave fires, but every schedule only gets re-armed once `world.timeS` actually reaches `windowS` (20s), never earlier, so nothing new can appear until the full window has genuinely elapsed regardless of how early the wave lands. Logged in DECISIONS.md along with why the stage times (3s/9s/15s, none of them named in GAME_DESIGN.md — only `windowS`=20 is) are spaced the way they are.
  `main.ts` requests it via `createWorld`'s new argument exactly when `profile.totalPassages === 0` at the moment `beginPassage()` runs — since `recordRunResult` increments that field the instant any Passage ends, it can structurally never fire twice for one profile, no separate "have I taught this player yet" flag needed.
  5 new tests (`tests/sim/firstRunTeaching.test.ts`): off by default, starts at stage 'slip' when requested, presents nothing at all before the Slip run's own trigger time, presents exactly one of each (Slip run, Gate pair, wave) in the correct order and nothing else (no Sealstacks, no ink pools) across the whole 20s window, and normal Director cadence measurably resumes afterward (not stuck suppressed forever). Verified in headless Chromium against the real player-facing flow (no `?debug=1`, no pre-seeded storage — a genuine first-ever session): screenshotted the Slip run, then the Gate pair, then Line growth after the recruit, confirmed every `/ui` screen stays `display: none` throughout (GAME_DESIGN.md §13's "no text panel appears," a real assertion against computed style, not just an eyeball check) with zero console/page errors.
  Ran the mandatory `npm run sim -- --runs 200`: byte-identical to the last committed report — the harness never passes `firstRunTeaching` (defaults false for every bot run), so this task's new code path never executes there either. **Phase 4's Tasks 4.1-4.5 all now land.** — Next session: 4.6 (the real balance pass) is the last of Phase 4 — sweep the Director/Slip budget until every §11 target in `BALANCE_REPORT.md` actually passes, which right now most of them don't (several were only ever measured, never tuned toward, across Tasks 2.7-4.5).
- [2026-08-08] 4.6 — The real balance pass, across this and part of a prior session. Built the instrumentation the previous N/A rows needed: per-Blot-class death attribution (`resolveLineContact` now returns `{strokesLost, crustInvolved}`, threaded through `killLineIfEmpty` into a new `World.deathCrustInvolved`, powering "Deaths from Crust") and a genuine multi-Passage meta-progression simulation (`src/balance/metaProgression.ts` — plays up to 150 Passages back to back with a real `Profile`, Gold Leaf carried and spent greedily between runs, powering "Runs to afford first upgrade"/"Runs to Inkstone level 40"). Found and fixed a real correctness bug in the harness's own "Strategy dominance" row: it was comparing median *Gold Leaf* across every strategy in the batch instead of median *distance* between exactly greedy-slips and greedy-kill, the comparison GAME_DESIGN.md §11 actually names — the old metric had been silently masking a real, large dominance failure as N/A or noise for as long as it existed (DECISIONS.md).
  Result: 9 of 11 §11 rows now PASS (all four zero-upgrades rows — length, peak Line, Gate deaths, Crust deaths — both meta-progression rows, First Seal broken, and Median peak Line at upgrades level 5). Two rows remain FAIL after an extensive sweep (30+ full 2000-run configurations measured this session, several genuinely chaotic/knife-edge regions found and characterized — DECISIONS.md has the detail): Strategy dominance (24.7% vs target ≤20%, close) and First Seal reached (38.0% vs target ≥70%, a real structural gap — none of the five harness bot strategies has Seal-attack awareness, so Seal fights currently cause ~0% of deaths even well past 75s, meaning nothing yet makes "70%+ still alive at 75s" and "median Passage length ≤85s" simultaneously achievable through Director tuning alone). Median Passage length at upgrades level 5 also stays FAIL at 131.8s (target 150-230s) — pushing the two relevant Inkstone tracks (grind/nib) further closes it but pushes peak Line at level 5 past its own 250 ceiling first. Split the remainder into a new Task 4.6b (`TASKS.md`) rather than block Phase 5 indefinitely — both remaining gaps need real code (Seal-dodging bot logic, possibly boss lethality tuning), not further config sweeping.
  Along the way, running the full test suite (not just the harness) surfaced and fixed real regressions the §11 table alone couldn't see: `tests/sim/press.test.ts`'s "winnable for at least some seeds" baseline (Task 3.3's own acceptance bar) had silently gone from 80% to 0/40 seeds, because tightening `director.slipBudget.perU` for the length/peak-Line targets also starves the Slip income a zero-upgrades Line needs *during* a Seal fight (GAME_DESIGN.md §8.2: "Seals do not block Slips") — backed `perU` off from 260 to 210, the least-generous value clearing the baseline with real margin. Also fixed three tests (`config.test.ts`, `director.test.ts`, `meta/economy.test.ts`) that pinned GAME_DESIGN.md's original starting-value literals directly and were guaranteed to break the moment this task legitimately retuned them — rewritten to check formula *shape* against `BALANCE`'s live values instead. And extended `sealCadence.test.ts`'s and `firstRunTeaching.test.ts`'s "keep the Passage alive to observe X" bot steppers with a Line-floor safeguard, since both files explicitly disclaim testing fight survivability but were incidentally relying on a zero-upgrades early game gentle enough for their (undefended or non-dodging) bots to survive to the point being tested — no longer true after this task's Director retune.
  `npm run verify` (typecheck + lint + all 436 Vitest cases + production build) is fully green. Final `BALANCE_REPORT.md` regenerated at `--runs 2000` (the canonical zero-upgrades batch); level-5 numbers verified separately via `--upgrades 5` and recorded in DECISIONS.md since the harness can only report one upgrade-level batch per invocation. **Phase 4 is now functionally complete** (4.6's own "all rows passing" bar isn't fully met, but is explicitly split into a scoped 4.6b rather than left silently unfinished) — per CLAUDE.md, continuing directly into Phase 5 (5.1 PWA) in this same session.
- [2026-08-08] 4.6b — Seal-dodging bot logic + a second lethality-tuning attempt, closing out the split from 4.6. `src/balance/bot.ts` gained `decideSealDodgeTargetX`, generalizing the exact per-boss safe-position logic each Seal's own zero-upgrades baseline test (smear/press/blank.test.ts) already used ad hoc into a shared function every harness strategy now runs at top priority (ahead of its own Blot/Slip targeting) whenever `world.seal !== null`: stand at the lane edge opposite the Smear's arm or the Blank's cone centre, or the average of the Press's gap centres; the Blank's erasure beam still has no dodge (targets the road, not the Brush). This makes "First Seal broken" a real measurement rather than the ceiling artifact it was before Task 4.6b (updated `report.ts`'s caveat text to say so).
  Confirmed empirically, not just predicted, that dodging structurally cannot move "First Seal reached" (unchanged at 38.0% before/after — it's entirely a pre-75s Director-survival question, a fully separate system from in-fight behaviour) and independently re-confirmed the prior session's `seals.hpBase` finding under this session's much-changed Director/economy state: even at ~4x the original value, Seal-caused deaths stayed at 0.0% and "First Seal broken" didn't move — zero-upgrades DPS is never the bottleneck regardless of Seal HP. Reverted the HP change (it also cost an unexplained small regression on "Deaths within 8s of a Gate"). Both rows stay FAIL — this counts as the "second independent tuning attempt, documented as structurally as-designed" 4.6b's own acceptance bar allowed for. Identified but didn't attempt the next concrete lever (decoupling Seal-attack damage from the shared `BALANCE.line.normalContactStrokeLoss` every boss's attack currently reuses — "First Seal broken" tracking "First Seal reached" almost exactly 1:1, ~98%, is the tell that nothing currently makes a *dodged* fight lose-able) — logged as a new Task 7.9 rather than left as a bare DECISIONS.md note with no tracking.
  `npm run verify` green (436 tests, dodging logic itself needed no new test coverage — it's exercised implicitly by every existing Seal-encounter test and the harness's own 2000-run batches). Final `BALANCE_REPORT.md` regenerated, still 9/11 §11 rows passing, both remaining FAILs now backed by two independent tuning attempts each. **Phase 4 (Tasks 4.1-4.6b) is done for this pass.** Continuing into Phase 5 (5.1 PWA) in this same response, per CLAUDE.md.
- [2026-08-08] 5.1 — PWA: manifest, service worker, generated icons, install prompt, Wake Lock, offline verification. `src/build/` is new (`png.ts` + `generateIcons.ts` + `generateServiceWorker.ts` + `verifyNoExternalFetch.ts`, compiled via the CLI's `tsconfig.cli.json` alongside `/balance`, same pattern `harness.ts` already established): a from-scratch PNG encoder (CRC-32 + PNG chunk framing + `node:zlib`'s DEFLATE, no `canvas` package — not in TECH_SPEC.md §2's devDependency list) rasterizes the exact triangle `render/strokes.ts`'s `drawHaraiGlyph` draws via a per-pixel point-in-triangle test, since Node has no real Canvas to hand that geometry to; produces `public/icons/icon-192.png`, `icon-512.png`, and a `icon-mask-512.png` with extra safe-zone padding, all gitignored (regenerated every build, never committed — "No image files," TECH_SPEC.md §7). `manifest.webmanifest`'s `icons` array now references all three with correct `sizes`/`type`/`purpose`.
  The service worker (`src/build/generateServiceWorker.ts`) runs *after* `vite build` (it needs the real hashed filenames) and writes `dist/sw.js` directly: precaches every built file at install, cache-first for hashed assets, network-first-with-cache-fallback for the document, and a version name that's a SHA-256 of the sorted precache list (deterministic — an unchanged rebuild produces an identical cache name, an actual content change doesn't) clearing every other cache on activate. `npm run build` now runs `generate-icons -> tsc -> vite build -> generate-sw -> verify-offline`, the last step (`src/build/verifyNoExternalFetch.ts`) scanning `dist/`'s real built JS for any `http(s)://` URL and failing the build outright if one exists (TECH_SPEC.md §10's "verify... that fails if any runtime fetch to an external origin exists in the bundle" — a build-pipeline gate rather than a Vitest test, since `npm run verify`'s own script order runs `test` before `build`; the URL-matching logic itself, `findExternalUrls`, is still a pure function with real Vitest coverage). Caught a real bug while wiring this up: the CLI script's `main()` was originally called unconditionally at module scope, so unit-testing `findExternalUrls` by importing it also silently ran a real filesystem scan (and could `process.exit(1)`) as an import side effect — fixed with the standard "compare import.meta.url against process.argv[1]" am-I-the-entry-module guard.
  `platform/pwa.ts` (previously an empty Task 5.1 stub) now holds `registerServiceWorker` (skipped in `npm run dev`, where `generate-sw` never runs), `createInstallPromptController` (captures `beforeinstallprompt`, exposes `promptInstall`/`dismiss` to `ui/screens/title.ts`'s new install line — dismissal persists via a bare `KeyValueStorage` key, not the `Profile` schema, since "already saw the nudge" is a device-local UI concern with no gameplay meaning and explicitly *shouldn't* survive a profile export/import), and `createWakeLockController` (acquired in `main.ts`'s `beginPassage()`, released on death, re-acquired on `visibilitychange` back to a visible `'playing'` tab — the Wake Lock API auto-releases on hidden, so without the re-acquire a backgrounded-then-returned-to long Passage would silently stop holding it). `index.html` also gained `<link rel="icon">`/`<link rel="apple-touch-icon">` pointing at the generated 192px icon — found missing during offline verification (see below) as a harmless but real console 404 for `/favicon.ico`, fixed for free.
  Verified end to end with headless Chromium (Playwright, already available in this environment, not added as a project dependency) rather than an actual Lighthouse run (`lighthouse`/`chrome-launcher` aren't approved devDependencies — logged in DECISIONS.md, along with the specific Lighthouse PWA checks manually confirmed instead): loaded the real built `dist/` via `vite preview`, confirmed the service worker reaches `activated` and takes control on reload, then set the browser context fully offline and reloaded — the title screen renders correctly (real text, zero console errors), and clicking "Begin a Passage" plays a real Passage (canvas render confirmed via screenshot) with no network at all.
  `npm run verify` green (440 tests — 4 new for `findExternalUrls`, `tests/build/verifyNoExternalFetch.test.ts`) including the full `npm run build` pipeline (icons -> typecheck -> vite build -> service worker -> offline-fetch gate). **Task 5.1 done.** Next: 5.2 (Performance) — the adaptive quality manager, `npm run perf`, and `PERF.md` against TECH_SPEC.md §8's budget.
- [2026-08-08] 5.2 — Performance: the adaptive quality manager, `npm run perf`, `PERF.md`. `src/render/quality.ts` (new) is a pure, fully unit-tested state machine (`tests/render/quality.test.ts`, 8 cases, driven by a fake clock so no test actually sleeps): tracks a rolling 30-frame real-time average, and after 2s sustained below 52fps steps down one of four quality levels in TECH_SPEC.md §8's exact named order — stipple dot count (the closest thing to a "particle" system this build has — `render/strokes.ts`'s density-block minority-class dots), then the ink trail's repaint frequency (skips alternating frames), then `sim/line.ts`'s `classifyForRender` density-block row thresholds (gained two optional override parameters, defaulting to `BALANCE.line`'s own values so every existing caller is unaffected — `BALANCE` itself stays frozen). Also recovers one level after a longer sustained comfortable frame rate (58fps for 5s) — not named in TECH_SPEC.md §8, but a degrade-only manager would mean one 2s hiccup permanently degrades quality for the rest of a session; logged as a reasoned addition in DECISIONS.md. `main.ts`'s `render` callback is the only caller: measures real wall-clock frame delta, feeds it in, and threads the resulting settings into `drawLine`/`drawInkTrail` — `update` (the fixed sim step) never touches it, so simulation rate is structurally never affected.
  `src/balance/perf.ts` (new, compiled via the CLI's `tsconfig.cli.json` alongside `/balance`'s other scripts) stress-tests TECH_SPEC.md §8's exact max entity counts (400 Strokes, 900 Blot) over 3000 measured `stepWorld` steps and writes `PERF.md` with p50/p95/p99/max. Found and fixed a real bug while building it: without actively "sustaining" the load (topping the Line and Blot pool back up to max every step), the stress scene's Line dies within a few hundred steps under `stepWorld`'s own ordinary attrition (the stress input never fires back), after which `stepWorld` permanently early-returns and most "measured" steps were actually free no-ops — visible as a suspicious p50 of 0.000ms before the fix, same root cause and same fix shape (an explicit stay-alive mechanism) as `sealCadence.test.ts`/`firstRunTeaching.test.ts`'s earlier fixes this same session.
  `npm run perf`'s reproducible numbers are simulation-only (no `CanvasRenderingContext2D` in Node without a forbidden devDependency — `PERF.md` says so explicitly) and land far under budget (p50 0.026ms vs. a 16.667ms/60fps frame budget) — genuinely useful as a lower bound, but read in isolation it would misleadingly suggest huge headroom. Gathered one real, render-inclusive supplementary data point by hand via headless Chromium (Playwright, environment-available, not a project dependency) actually playing a Passage at the same max load via `?debug=1`'s existing Digit5/Digit8 debug hooks: p50 16.7ms — right at the 60fps budget, in software-rendered headless Chromium — confirming render cost, not simulation cost, is where this build's frame budget actually goes. Hardcoded into `perf.ts`'s report template (dated, clearly one-off) so it stays visible in the committed `PERF.md` even though the script itself can't reproduce it.
  Real TECH_SPEC.md §8 verification (sustained 60fps on a Snapdragon-695-class device, cold start ≤1.5s, memory ≤180MB) isn't possible in this environment — `PERF.md` documents each as an honest shortfall rather than a fabricated pass, per this task's own acceptance bar ("the §8 budget is met or the shortfall is documented with the measurement"). The one budget item that *is* directly verifiable locally — initial payload ≤400KB gzipped — passes comfortably (28.31KB gzipped as of this build).
  `npm run verify` green (450 tests — 10 new: 8 for the quality manager, 2 for `classifyForRender`'s new override parameters). **Task 5.2 done.** Next: 5.3 (Accessibility) — Shapes-Only toggle, greyscale legibility, reduced-motion, focus states, ≥44px hit targets.
- [2026-08-08] 5.3 — Accessibility. Task 4.3's Settings toggles (`shapesOnly`/`reducedMotion`) already persisted but had no consumer (DECISIONS.md flagged this explicitly at the time) — this task finally builds what they gate. `render/strokes.ts` gained `drawShapesOnlyMark` (a small 1/2/3-dot count per class, stamped in `PALETTE.deep` on every individually-drawn Stroke and every Slip when `shapesOnly` is on — GAME_DESIGN.md §12's exact wording), exported `shapesOnlyMarkCount` purely so `tests/render/strokes.test.ts` can assert the three classes get distinct counts without a mock Canvas context (2 new tests). `render/trail.ts`'s `drawInkTrail` gained a `reducedMotion` parameter that zeroes its wobble/jitter terms — exactly the spot Task 2.10's own comment already flagged as "Task 5.3's job." Both wired into `main.ts`'s render loop from `profile.settings`.
  `index.html` gained an explicit `:focus-visible` rule (a 2px goldLeaf outline on every `<button>`/`<input>`/`<textarea>`, visible only for keyboard/assistive-tech focus, never a pointer tap) — CSS-in-JS inline styles (every `/ui` widget's own approach) can't express a pseudo-class, so this is the one global stylesheet rule in the whole project. `widgets.ts`'s `createTrackRow` (the Inkstone's buy-a-level row) gained an explicit `minHeight: 44px` — its actual content height already cleared that in practice, but every other interactive widget already stated the ≥44px rule explicitly rather than relying on incidental content size, so this one now does too.
  Verified in headless Chromium (Playwright, environment-available, not a project dependency): enabled both toggles via Settings, built a big mixed-class Line via the existing `?debug=1` hooks, screenshotted normally and again with a CSS `grayscale(100%)` filter applied to the whole page — **this task's own acceptance bar** ("a greyscale screenshot still distinguishes all three classes"), confirmed directly: tick/block-with-dots/sliver read as three unmistakably different shapes with zero colour information. Also confirmed Tab-focusing the title screen's "Begin a Passage" button shows the new goldLeaf outline (`outlineStyle: solid`, `outlineColor: rgb(201, 162, 39)`), and zero console errors throughout.
  Discovered a real gap while wiring reduced-motion, not introduced by this task: GAME_DESIGN.md §12's "motion rules" (camera shake on Seal impacts/Flourish, an ink-splat-plus-scale-pop hit-feedback system) were never built by any earlier task — nothing in `TASKS.md` ever named them as a deliverable, so "halve shake" currently has nothing to act on. Logged as a new Task 7.10 rather than built unscoped inside an accessibility task, or silently ignored.
  `npm run verify` green (452 tests). **Task 5.3 done.** Next: 5.4 (Android) — Capacitor project, portrait lock, back-button handling, zero permissions, haptics, debug APK, `README.md` build docs.
- [2026-08-08] 5.4 — Android, the last task in Phase 5. Installed `@capacitor/core`/`cli`/`android`/`app`/`haptics` as devDependencies (TECH_SPEC.md §2's explicit "Capacitor (dev dependency only)" allowance) and ran `npx cap add android` to scaffold the full native project, now committed under `/android`. Fixed the manifest to match TECH_SPEC.md §11 exactly: removed Capacitor's default `INTERNET` permission (this app never calls the network at runtime — every asset ships inside the APK), added `android:screenOrientation="portrait"` on the one Activity. Found and fixed two real gaps in the generated scaffold along the way, not caused by this task but discovered by it: `styles.xml` referenced `@color/colorPrimary`/`colorPrimaryDark`/`colorAccent` with no `colors.xml` actually defining them (would have failed to compile), and the default splash screen used a set of placeholder white PNGs — replaced with a real `colors.xml` mapping onto `render/palette.ts`'s own values and a plain-colour splash background (no image file needed, keeping TECH_SPEC.md §7's "no image files" rule intact for Android too).
  Back button (`@capacitor/app`'s `backButton` event, inert outside a real Android WebView) maps to Settings→Title, `App.minimizeApp()` while mid-Passage (preserves `World` state entirely — genuinely "pause," not a new pause UI this task never asked for), and `App.exitApp()` everywhere else — never a straight exit from a Passage, matching TECH_SPEC.md §11 exactly. Haptics (`@capacitor/haptics`) wired to all three named triggers: Flourish via a new `flourishTriggered` /sim event (`sim/events.ts` — the one addition beyond Task 4.4's audio-only event scope, since a real sweep has no other externally-observable signal to diff against), and Seal-impact/Line-loss both via a direct `world.line.strokes.length` diff in `main.ts`'s own `update` callback (no new event needed there — distinguished by whether `world.seal !== null` at the moment of loss, with a heavier haptic style for the Seal case).
  `src/build/generateIcons.ts` (Task 5.1) extended, not duplicated, to also render Android's five `mipmap-*` launcher icon sizes using the identical Harai-glyph rasterizer — every icon anywhere in the project is the same brushstroke at a different size. Regenerating icons surfaced a real false positive in Task 5.1's `verify-offline` build gate: `@capacitor/core`'s own bundled MIT-licence comment contains a literal `https://capacitorjs.com/` string, which the external-URL scanner correctly matched but isn't an actual network call — fixed with a small, manually-verified `KNOWN_BENIGN_URLS` allowlist in the CLI scanner only, keeping the underlying pure matcher (and its existing tests) untouched.
  Genuinely attempted the debug APK build, not just documented it as skipped: `./gradlew assembleDebug` (tried both `--offline` and networked) confirmed this environment has no Android SDK and its network policy returns `403 Forbidden` for `dl.google.com` (the Android Gradle Plugin's Maven host) — the exact failure point, recorded in `README.md`'s new Android section along with the full build/release commands for whoever runs this on a real machine. Matches TECH_SPEC.md §11's own explicit allowance for exactly this case: commit the full project and docs, note the APK as unverified, don't block on it.
  `npm run verify` green (452 tests — haptics/back-button/App-plugin code needed no new unit tests, same "verify browser-only behaviour by eye" convention every other `/platform` integration in this project already follows; re-verified offline play end to end in headless Chromium with Capacitor's web shims now bundled in, zero console errors). **Phase 5 (Tasks 5.1-5.4) is done.** Per CLAUDE.md's "what 'done' means for the whole project": `npm run verify` is green and the PWA is installable/offline-verified, but `BALANCE_REPORT.md` doesn't yet show every §11 target passing (2 of 11 remain FAIL, extensively documented across Tasks 4.6/4.6b) and no `PLAYTEST_NOTES.md` exists yet — both still needed before Phase 6 can begin. Next: write `PLAYTEST_NOTES.md` (an honest assessment of feel, with specific timings, per CLAUDE.md's own bar), then decide whether the two remaining §11 gaps (Task 7.9's Seal-damage lever, the Strategy-dominance knife-edge) are worth another dedicated pass before treating "done" as reached.
- [2026-08-08] `PLAYTEST_NOTES.md` — the last piece of CLAUDE.md's "what 'done' means for the whole project" checklist. Grounded in two real sources, not invented: a genuine headless-Chromium playthrough driving real keyboard input (`ArrowLeft`/`ArrowRight`/`Space`, no `?debug=1` shortcuts, no bot), and `BALANCE_REPORT.md`'s real 2000-run statistics. The real playthrough died in 11s to "A Gate cost too much" at 243u/peak-Line-3 — a genuine, unscripted data point landing exactly inside the harness's own "Deaths within 8s of a Gate" 11.0%-of-runs bucket, not a bug; noted the profile used wasn't fresh, so it didn't get Task 4.5's scripted first-20-seconds softening a genuinely first-ever player would. Called out two feel gaps plainly rather than glossing over them: Seal encounters are still out of reach for most zero-upgrades runs (the same First-Seal-reached gap Tasks 4.6/4.6b already tracked as FAIL), and GAME_DESIGN.md §12's camera-shake/hit-feedback system (discovered missing during Task 5.3, tracked as Task 7.10) is a real, feelable absence in actual play, not just a missing checklist item.
  With this, every item in CLAUDE.md's "what 'done' means for the whole project" is now true *except* `BALANCE_REPORT.md` showing every §11 target passing (9 of 11 do; Strategy dominance and First Seal reached remain FAIL, each backed by two independent, extensively-documented tuning attempts across Tasks 4.6/4.6b). Per CLAUDE.md's own instruction ("Then, and only then, begin Phase 6"), Phase 6 stays locked until that's resolved or a further attempt is made — not declaring the whole project "done" while an explicit bar is knowingly unmet.
- [2026-08-08] 7.9 — Decoupled each Seal boss's attack damage from `BALANCE.line.normalContactStrokeLoss` (the shared ordinary-Blot-contact constant every boss's attack previously reused), per the lever Task 4.6b identified but never tried. Split into three independent config values (`seals.smear/press/blank.attackStrokeLoss`) rather than one shared number: empirically, raising the old shared value (1) to even 2 collapsed Smear's own isolated zero-upgrades baseline test (`smear.test.ts`, Task 3.2's acceptance bar — a fresh 3-Stroke Line against a deliberately dumb, never-returns-to-centre bot) from a 25% to a 0/40 win rate, a real regression with no §11 benefit, since that isolated test's starting Line is nowhere near the ~30-40 Strokes a real harness run has grown to by its first Seal encounter at ~75s. The identical change left Press's isolated test completely unaffected (its bot dodges every slam, 0 hits regardless of this value) and Blank has no isolated win-rate test to protect at all. Kept Smear at the pre-existing 1, moved Press and Blank to 4 each — high enough to matter against a harness bot's imperfect dodging at real Seal-encounter Line sizes.
  Also checked the premise against the current `BALANCE_REPORT.md` before spending further tuning effort: "First Seal broken" (the row this task targeted) was already PASS (37.3%, comfortably mid-band) going in — this change's job was to make the number independently tunable and give real Seal fights teeth, not to chase an already-passing metric further. Re-ran the full 2000-run harness after landing the split: "First Seal broken" stays PASS (37.3%, unchanged), "Deaths within 8s of a Gate" and "Deaths from Crust" both stayed PASS too (11.0%/17.1%, within noise of the prior batch — a 200-run intermediate check had shown Gate-deaths wobble to 12.5%/FAIL, confirmed as pure small-sample noise by the larger run, not a real regression from this change). Same 9/11 §11 PASS as before this task: "Strategy dominance" and "First Seal reached" remain FAIL, unaffected — both are Director/pre-fight-survival questions, structurally outside anything Seal-attack damage can touch, as Task 4.6b already established.
  `npm run verify` green (452 tests, build clean). Full decision record in `DECISIONS.md`. **Task 7.9 done.** Phase 6 (Playtest & Polish, GAME_DESIGN §... locked per CLAUDE.md until every §11 row passes) stays locked — the two remaining FAILs are the same structural gaps identified across two independent tuning attempts in Tasks 4.6/4.6b, with no new lever currently identified for either. Continuing into the rest of Phase 7's freely-available tasks: next is 7.8 (wire Gates' Temper track into the sim — accumulated but never applied) or 7.10 (camera-shake/hit-feedback system, the other tracked feel gap from `PLAYTEST_NOTES.md`).
- [2026-08-08] 7.8 — Wired Gates' Temper track into the sim: `gates.ts`'s `TemperState` (rateStacks/rangeStacks/splashStacks/wetnessCapStacks) was accumulated on every Temper Gate resolution ever since Task 2.6 but never actually applied anywhere — a gap Task 4.2's own `upgradeEffects.ts` comment discovered and explicitly deferred to this task, leaving the multiplier parameters (`rateMultiplier`, `rangeMultiplier`, etc.) already in place at the right call sites for exactly this. New `computeTemperEffects(world.temper)` in `gates.ts` turns the four stack counts into a `rateMultiplier`/`rangeMultiplier`/`splashMultiplier`/`wetnessCapBonus` bundle, composed alongside `world.upgradeEffects`'s equivalent fields at `stepWorld`'s existing fire-rate, projectile-range, and Wetness-cap call sites (multiplicative for rate/range/splash, additive for the Wetness cap — same composition shape Task 4.2 already established). Splash had no existing multiplier anywhere (no Inkstone splash track) — added a new `splashMultiplier` parameter to `projectiles.ts`'s `updateFiring`/`spawnProjectile`, deliberately appended as the *last* parameter rather than inserted next to `rangeMultiplier`, so `tests/sim/projectiles.test.ts`'s one existing positional call keeps meaning what it already meant.
  New `tests/sim/temperEffects.test.ts` (4 tests) proves all four Temper stats measurably change `stepWorld` behaviour, mirroring `worldUpgrades.test.ts`'s existing "every Inkstone track measurably changes stepWorld behaviour" pattern — set `world.temper` directly (the real path, `applyGateEffect`, is already covered by `gates.test.ts`) so what's under test is `stepWorld`'s own composition. The Wetness-cap test originally simulated a real drain-then-refill through `stepWorld` like the Well-Inkstone test it was modelled on, but Temper's stack-capped bonus (125 at max stacks, versus Well's ~40) needed a drain phase long enough that the undefended zero-upgrades Line actually died to ordinary Blot contact mid-test, silently freezing every subsequent `stepWorld` call at a stale reading (the same "stepWorld permanently early-returns once isDead is set" trap earlier sessions hit with `sealCadence.test.ts`/`firstRunTeaching.test.ts`) — fixed by setting the drained `WetnessState` directly instead, since the drain path itself is already covered elsewhere and isn't what this test means to verify.
  `npm run verify` green (456 tests, build clean). Re-ran the full 2000-run balance harness: no §11 row regressed (a 200-run intermediate check showed "Deaths within 8s of a Gate" wobble to 12.5%/FAIL, confirmed as ordinary small-sample noise once the full 2000-run batch landed back at 9.8%/PASS — Temper Gates are resolved by every harness bot already, so this genuinely exercised the new wiring). Same 9/11 §11 PASS as before this task. **Task 7.8 done.** Continuing into the rest of Phase 7's freely-available tasks next.
- [2026-08-08] 7.10 — Built the camera-shake/hit-feedback system GAME_DESIGN.md §12's "motion rules" describe, discovered missing during Task 5.3 and tracked as a gap ever since. Camera shake (new `render/shake.ts`) triggers on exactly the two sources §12 names — Flourish (its own event) and Seal impact (the same Stroke-loss-while-fighting-a-Seal diff `main.ts` already computes for haptics, reused rather than duplicated) — as a ≤4px, ≤180ms decaying random-direction jitter, applied as a `ctx.translate` at the final `layers.compositeInto` blit step around only the 3D-scene layers (sky/water, road+trail, actors), never the persisting road-trail canvas's own coordinate system (which would have baked a jittery seam into its accumulated ink permanently) and never HUD/vignette (which stay screen-locked). `reducedMotion` halves the amplitude rather than disabling it, per §12's own instruction.
  Hit feedback (an ink splat + 60ms scale pop, "never a flash") is scoped to Blot — the dominant on-screen volume (up to 900) and where "No screen-wide particle spam" (the sentence immediately following in §12) matters most. A new `lastHitAtS` field on `Blot` (set by `collision.ts` on every projectile hit, splash included) drives a small pure function, `render/blot.ts`'s `computeHitPop`, deliberately factored out of the actual canvas calls so it's unit-testable the same way `road.ts`'s `computeDashSpans` already is — this codebase's convention is to test the pure math behind a draw call, never to mock `CanvasRenderingContext2D`. No new colour anywhere: the splat is the same `PALETTE.blot` fillStyle as the Blot itself, just a larger, fading echo — that's what "never a flash" rules out.
  New tests: `tests/render/shake.test.ts` (5 tests, covers the ≤4px cap, ≤180ms decay, and the reducedMotion halving) and `tests/render/blot.test.ts` (4 tests, covers `computeHitPop`'s decay shape). Verified in headless Chromium: zero console errors through a Flourish trigger and a sustained 900-Blot debug-wave firefight, and the road-trail layer stays visually stable after a shake window elapses (confirming the compositeInto-only approach doesn't bake in a seam). Honestly did not verify the Blot pop/splat is legible by direct screenshot — Blot's own colour is deliberately near-black ("always the darkest thing on screen," GAME_DESIGN.md's palette table) at low contrast against the background, making a static screenshot a poor tool for judging a 60ms cosmetic regardless of correctness; leaned on the new unit tests instead and logged this the same way `PLAYTEST_NOTES.md` logs its own unverified-by-eye items, rather than claiming a look that wasn't actually conclusive.
  `npm run verify` green (465 tests, build clean). 2000-run balance harness came back byte-identical to the pre-task report (purely cosmetic — no gameplay path ever reads `lastHitAtS`), confirming zero behavioural drift. **Task 7.10 done.** This closes the last item PLAYTEST_NOTES.md flagged as a real, feelable gap. Remaining open items: the two structural §11 FAILs (Strategy dominance, First Seal reached, both Director/pre-fight-survival questions per Tasks 4.6/4.6b) and the rest of Phase 7's optional-polish tasks (7.1-7.7). Continuing into the next available task.
- [2026-08-08] 7.7 — Vendored the two real OFL fonts TECH_SPEC.md §7 names, replacing Task 2.11's system-font fallback everywhere DOM text appears. Sourced official release TTFs for Shippori Mincho B1 and Zen Kaku Gothic New from the `google/fonts` GitHub repo (reachable through this environment's proxy even though `fonts.google.com` itself is blocked — confirmed via the proxy's own status endpoint), installed `fonttools`/`brotli` for this session only (a one-time tool to produce a committed static asset, not a project devDependency), and subset each to Latin+digits+punctuation with `pyftsubset` — all four resulting files (Regular + a bold weight per family) land at 12-37KB, comfortably under the 40KB-each target. Vendored two weights per family rather than one (Shippori Regular+ExtraBold, Zen Kaku Regular+Bold) so the app's existing `fontWeight: '700'` DOM usages and GAME_DESIGN.md's "Gate numbers... at Shippori's heaviest weight" language both get a real bold face instead of the browser's synthetic-bold fallback — reasoning logged in DECISIONS.md against a literal read of TECH_SPEC's own file-tree comment.
  `@font-face` rules added to `index.html` (`font-display: block` for Shippori, `swap` for Zen Kaku, per TECH_SPEC.md §7's exact wording); `ui/widgets.ts`'s font stacks now lead with the real family names, keeping the system stack as a legitimate fallback tail. Verified in headless Chromium: `document.fonts` confirms both real faces load (not the fallback), the title heading's computed style resolves to the vendored Shippori face, and — re-running Task 5.1's offline-verification method — an offline reload after the service worker's precache (grew 9→13 entries) still renders the real fonts with zero console errors. Built payload grew ~95KB (already-compressed woff2), nowhere near the 600KB total budget.
  Discovered, not fixed here: GAME_DESIGN.md §12's "Gate numbers are the loudest type in the game" describes rendering that was never actually built anywhere — Gates today are plain coloured quads with no number/text at all, so a player can't currently read what a Gate does before choosing. Out of scope for a font-vendoring task; logged as new Task 7.11.
  `npm run verify` green (465 tests, build clean). **Task 7.7 done.** Remaining open items: the two structural §11 FAILs, Task 7.11 (Gate-number rendering, just discovered), and Phase 7's other optional-polish tasks (7.1-7.6). Continuing into the next available task.
- [2026-08-08] 7.11 — Built the Gate-number rendering GAME_DESIGN.md §12 describes ("Gate numbers are the loudest type in the game — set them enormous, at Shippori's heaviest weight, with a bone stroke on the vermilion fill"), discovered during Task 7.7 to have never been built anywhere — Gates were plain coloured quads with no text at all, so a player had no way to read what a Gate pair actually does before choosing a side. New pure `gateEffectLabel` (`sim/gates.ts`) derives §7.2's exact literal display strings (`×2`, `→ Tome`, `Rate +20%`, ...) from `BALANCE.gates`'s own live values rather than a second hardcoded copy; a companion `STROKE_CLASS_DISPLAY_NAME` map (`sim/stroke.ts`) supplies the capitalised class names Conversion labels need.
  `road.ts`'s `drawGateHalf` draws the label in the now-vendored Shippori Mincho B1 ExtraBold face (no new font-load call needed — the title screen already guarantees it's loaded by the time gameplay can start), vermilion fill with a bone stroke, applied to every non-Sealed family since §12's typographic rule is stated for Gates as a UI element, not scoped to numerals alone. Caught and fixed a real overlap bug by actually looking, not just trusting the math: an early version sized the font purely from on-screen scale, which let long labels (`Wetness cap +25`) bleed into the neighbouring gate's label at realistic distances — fixed with a measured (`ctx.measureText`), not guessed, shrink-to-fit against 92% of the door's own projected width. Sealed gates draw a small stamped ring-and-dot mark instead of text, keeping their effect genuinely hidden pre-contact per §7.2.
  Verified in headless Chromium across many real spawned Gate pairs: arithmetic/conversion/temper labels all read cleanly with zero overlap, and a caught Sealed gate showed only the mark. New unit tests pin the exact §7.2 strings literally (the Gates arithmetic/temper tables are confirmed unchanged since Task 2.6, unlike the Director/economy values Task 4.6 legitimately retuned, so a literal pin here is a real drift-catcher). `npm run verify` green (479 tests, build clean); 2000-run harness byte-identical to the pre-task report.
  **Task 7.11 done.** This closes the last gap discovered this session. Remaining open items: the two structural §11 FAILs (Strategy dominance, First Seal reached — both Director/pre-fight-survival questions, unchanged across Tasks 4.6/4.6b's two independent tuning attempts) and Phase 7's other optional-polish tasks (7.1-7.6: daily seed, replay, statistics screen, photo mode, palette variants, colour-blind sim tool). Every item in CLAUDE.md's "what 'done' means for the whole project" checklist is true except the full §11 pass, which stays the honest, tracked blocker on Phase 6.
- [2026-08-08] 7.6 — Built `npm run check-colorblind` (`src/build/checkColorblind.ts`), automating GAME_DESIGN.md §12's own colourblind test method ("render greyscale, confirm classes remain distinguishable") instead of leaving it a one-off manual headless-Chromium check re-verified by hand each time (how Task 5.3 originally checked it). No `canvas` package available (not in TECH_SPEC.md §2's devDependency list), so it rasterizes the three Stroke glyphs against a flat pixel buffer via point-in-polygon testing — the same technique `generateIcons.ts` already uses for the app icon's Harai triangle, generalized here to handle Tome's quadrilateral too. The actual pass/fail gate is a colour-transform-independent silhouette (Jaccard) distance between each class pair's binary shape mask; fill-colour distance under greyscale and protanopia/deuteranopia/tritanopia simulation matrices is reported separately for context, since GAME_DESIGN.md §4/§12's rule is specifically that colour is allowed to fail as long as shape doesn't — gating on colour would test the wrong thing.
  First attempt tried sharing the glyph geometry with `render/strokes.ts` via a new export in `/sim/stroke.ts` — reverted after `config.test.ts`'s existing "no stray gameplay magic numbers in /sim" check (Task 1.6) correctly caught the new raw literals; cosmetic render geometry isn't gameplay-balance data and doesn't belong routed through `BALANCE` just to satisfy that rule mechanically. Landed on the same tradeoff `generateIcons.ts` already established for its own copy of this exact triangle: `checkColorblind.ts` duplicates the geometry with an explicit cross-reference comment, since it compiles under `tsconfig.cli.json` (no /render in that program). Along the way, refactored `strokes.ts`'s three separate glyph-drawing functions into one shared `strokeGlyphPolygon` (pure geometry) + `drawGlyph` — verified visually identical to the pre-refactor rendering in headless Chromium across all three classes at real in-game scale.
  New unit tests (`tests/build/checkColorblind.test.ts`) pin the three real glyphs' pairwise silhouette distances above a floor — a genuine regression catcher for future glyph tweaks, not just a report generator. `npm run verify` green (484 tests, build clean); this task never touched `/sim` gameplay logic, so no balance-harness re-run was needed.
  **Task 7.6 done.** Remaining open items: the two structural §11 FAILs and Phase 7's remaining optional-polish tasks (7.1-7.5: daily seed, replay, statistics screen, photo mode, palette variants).
- [2026-08-08] 7.1 — Built the daily seed + shareable result string, a feature GAME_DESIGN.md doesn't name (this task's own design). New `src/meta/dailySeed.ts`: `computeDailySeed` hashes the UTC calendar date with a from-scratch FNV-1a into the same kind of plain seed number `Date.now()` already produces (deliberately not routed through the seeded-RNG `RngRegistry` — it only picks *which* seed a Passage starts from, same role as `Date.now()`); `dailyDayNumber` counts days since the Unix epoch rather than inventing a game-specific launch-date epoch; `formatDailyShareText` produces a compact, comparable text block (not an emoji grid — this isn't a discrete-guess puzzle).
  Title screen gained a secondary "Today's Passage — Day #N" link (deliberately not competing with the primary "Begin a Passage" button); the summary screen gained a "Copy result" row, visible only for a Passage that used the daily seed, writing the share text via `navigator.clipboard.writeText` from a real button click (no network, no submission, no accounts — the player pastes it wherever they choose) and flashing "Copied" briefly. A Daily Passage explicitly skips GAME_DESIGN.md §13's scripted first-run opening even on a brand-new profile, so a first-time player's daily result stays comparable with everyone else's.
  Caught and fixed a real latent bug while wiring `beginPassage`'s new optional seed parameter: two existing call sites passed the bare function reference straight to a DOM click handler (`onPlay: beginPassage`), which would have silently received the click's `MouseEvent` as the seed argument — `tsc` never flagged it (TypeScript's callback-arity checking allows a function with fewer required params to satisfy a stricter `() => void` field). Both are now explicit `() => beginPassage()` wrappers.
  Verified end-to-end in headless Chromium: the real UTC day number renders on the title screen, and a forced death during a Daily Passage produced a clipboard write matching the exact expected format. `npm run verify` green (494 tests, build clean); this task never touches `/sim`, so no balance-harness re-run was needed.
  **Task 7.1 done.** Remaining open items: the two structural §11 FAILs and Phase 7's remaining optional-polish tasks (7.2-7.5: replay, statistics screen, photo mode, palette variants).
- [2026-08-08] 7.2 — Built replay-from-seed: a recorded input tape played back through the real engine. Recording is exactly the `WorldInput` `stepWorld` already consumes each fixed step, appended as `update()` produces it — the loop's existing determinism (fixed timestep, seeded RNG per concern) already guarantees seed + tape reproduces a Passage bit-for-bit, so the only new work was recording/storing/replaying the tape, not the reproduction mechanism itself. New `tests/sim/world.test.ts` case proves this holds for real varying input (not just the existing trivial all-zero-input determinism test): records a scripted-but-non-constant lateral/holding sequence, replays it against a fresh same-seed World, asserts an exact match, and includes a negative control (a different tape must actually diverge) so the test can't pass vacuously.
  New `'replaying'` `AppState` (distinct from `'playing'` — different input source, different back-button semantics) reuses a shared `stepAndReact` helper (step/haptics/shake/music) so real play and replay only differ in where input comes from and what happens when it ends. A replay never calls `handlePassageDeath` — no `recordRunResult`, no `saveProfile`, no Gold Leaf — since the run's result already happened for real the only time it can count; replaying it must not double it. Summary screen gained a "Watch replay" link, always available after any real Passage; ends automatically back at Title when the tape is exhausted or the World dies again, and the back button during a replay also ends it cleanly. Scoped to in-memory, single-most-recent-run only (not persisted across reloads, not a saved library) — TASKS.md's own brief only asks for the mechanism to work, and a full replay library is closer to Task 7.3's territory.
  Verified end-to-end in headless Chromium: a real recorded run's ~21s death timing reproduced almost exactly on replay, zero console errors, clean automatic return to Title. `npm run verify` green (495 tests, build clean); this task never touches `/sim` source (only adds a test), so no balance-harness re-run was needed.
  **Task 7.2 done.** Remaining open items: the two structural §11 FAILs and Phase 7's remaining optional-polish tasks (7.3-7.5: statistics screen, photo mode, palette variants).
- [2026-08-08] 7.3 — Built the Statistics screen, driven entirely by data `Profile` already tracks. New pure `src/meta/statistics.ts` (`computeRunHistoryStats`) aggregates TECH_SPEC.md §3's existing 40-run rolling `runHistory` into median/average/window-best distance, median peak Line, and a chronological distance list for a chart — `null` for an empty history (a real empty-state message, not a table of zeroes). Deliberately did not add a death-cause breakdown, which would need a real schema migration (`RunHistoryEntry` doesn't currently record it per-run) for a feature TASKS.md's brief doesn't actually ask for — scoped this pass to what's already there, logged as a clean follow-up if ever wanted.
  New `ui/screens/statistics.ts`, reached via a title-screen link grouped with Daily and Settings (Statistics is content to look at, not a mutable setting, so it lives with Title's other secondary destinations rather than inside Settings). Shows lifetime totals plus the rolling-window stats and a plain DOM bar chart of recent distances (styled divs, no canvas — kept consistent with every other `/ui` screen being DOM-only). New `'statistics'` `AppState`; back-button treats it like Settings.
  Verified by actually building real history rather than trusting the aggregation in isolation: played two real Passages to death in headless Chromium, reloaded the page (proving `runHistory` genuinely round-trips through `localStorage`), and confirmed the screen showed correct totals, a correctly-scaled chart, and correct aggregate numbers matching the two real runs — zero console errors.
  `npm run verify` green (500 tests, build clean); this task never touches `/sim`, so no balance-harness re-run was needed.
  **Task 7.3 done.** Remaining open items: the two structural §11 FAILs and Phase 7's last two optional-polish tasks (7.4: photo mode, 7.5: palette variants).
- [2026-08-08] 7.4 — Built photo mode: renders the ink trail of your best (session) Passage as a downloadable PNG scroll. New `render/scroll.ts` silently replays a Passage's seed + input tape (Task 7.2's `Replay` shape, extracted into shared `meta/replay.ts`) off-loop and paints each step's trail wash onto one tall, non-fading canvas indexed by distance — the whole Passage unrolled start-to-death as a single image, something no live moment of actual play ever shows at once. `trail.ts`'s width/colour formulas are now exported and reused directly rather than duplicated, so the scroll can't drift from what live play actually renders. Downloads via the browser's native `canvas.toBlob('image/png')` — no custom encoder needed (unlike the Node-only icon generator). "Best" is session-scoped, not persisted to Profile, to avoid taking on real localStorage weight for a Phase-7 polish task.
  Fixed a real, load-bearing gap while wiring the title-screen entry point: Title had always been reachable only once per session (nothing on Inkstone leads back to it), which was fine when Title held nothing worth revisiting mid-session — but Daily/Statistics/Photo Mode (7.1/7.3/7.4) changed that, silently making all three unreachable after a player's first Passage. Added a secondary "Menu" link on the Inkstone screen (Play stays the one primary exit, so the two-tap loop is untouched) to fix this for all three features at once.
  Caught two real bugs by actually downloading and opening generated PNGs, not trusting the math: a fixed-height per-step paint left gaps once distance-per-step exceeded it at a legible scale (fixed with a per-step slice height computed from the actual distance delta), and the initial 3px/unit scale produced a nearly-illegible 33px-wide image (raised to 10px/unit). A third test scenario surfaced a real pre-existing Task 7.2 limitation, not a new bug: debug hooks mutate World outside the recorded input tape, so replay can never see them — inert in real play for the same reason.
  `npm run verify` green (500 tests, build clean); this task never touches `/sim`, so no balance-harness re-run was needed.
  **Task 7.4 done. All of Phase 7's items from this pass are now complete except 7.5 (palette variants).** Every item in CLAUDE.md's "what 'done' means for the whole project" checklist is true except the full §11 pass (2 of 11 targets remain structural FAILs, extensively documented across Tasks 4.6/4.6b, unaffected by anything in this session's Phase-7 work).
- [2026-08-08] 7.5 — Built two unlockable palette variants (`nocturne`, at 500u best distance; `vermeil`, at 3 total Seals broken) alongside the existing `default`, satisfying GAME_DESIGN.md §12's palette table without breaking its colourblind legibility rule. `render/palette.ts`'s `PALETTE` stays the same object every consumer already reads by property access (`PALETTE.slate`, etc.) — `setPaletteVariant` mutates it in place via `Object.assign` rather than replacing the reference, so all ~14 existing call sites across `/render` and `/ui` needed zero changes. A small `onPaletteChange` pub-sub covers the two places with derived caches computed at module load time (`strokes.ts`'s `CLASS_COLOR`, `trail.ts`'s `CLASS_RGB` — the latter reads the former, so ES module import order guarantees correct listener firing order). `goldLeaf` and `blot` are identical across all three variants per §12's own "currency and only currency" / "always the darkest thing on screen" rules; only slate/deep/bone/jade/vermilion vary.
  Unlock logic (`isPaletteVariantUnlocked`, `paletteVariantUnlockLabel`) takes plain primitive stats, not the `Profile` type, so `/render` never needs to import `/meta` — kept the dependency arrow one-directional. `Profile` gained a `settings.paletteVariant` field via `sanitizeProfile`'s existing field-by-field-defaults pattern (no formal migration needed, matching every other optional-field addition this project). New `ui/widgets.ts` `createOptionRow` (a "pick one of a small named set" row, generalised from `createTrackRow`'s selectable/inert-when-unavailable shape) drives a new Settings-screen picker showing all three variants, locked ones greyed out with their requirement text. `main.ts` calls `setPaletteVariant` at bootstrap and on every settings change/import, so a swap applies live without a reload.
  Confirmed by re-reading `checkColorblind.ts` (Task 7.6) rather than re-running anything: its legibility gate is a colour-transform-independent silhouette (Jaccard) distance between Stroke-class shapes, so it already covers every possible palette variant automatically — no separate re-validation needed to satisfy this task's "(must keep §12 legibility rules)" clause.
  Verified in headless Chromium: Settings screen shows all three variants with correct locked/unlocked state, a live in-game colour swap on selection with zero console errors, and unlock thresholds confirmed by directly seeding `localStorage` with qualifying/non-qualifying profile stats. New `tests/render/palette.test.ts` (9 tests) pins the mutate-in-place mechanism, the fixed-goldLeaf/blot invariant, pairwise Stroke-class colour distinctness per variant, the `CLASS_COLOR` propagation, listener firing, and both unlock thresholds. `npm run verify` green (509 tests, build clean); this task never touches `/sim`, so no balance-harness re-run was needed.
  **Task 7.5 done — this closes out Phase 7 (7.1-7.11) entirely.** Every item in CLAUDE.md's "what 'done' means for the whole project" checklist is now true except the full §11 pass (2 of 11 targets remain structural FAILs: Strategy dominance and First Seal reached, extensively documented across Tasks 4.6/4.6b/7.9). Next candidate per TASKS.md is 5.5 (ship review, including a `v1.0.0` tag) — Phase 6 stays explicitly locked ("begin only when told"). The `v1.0.0` tag sits in real tension with CLAUDE.md's own "done" checklist while §11 has open FAILs; continuing into 5.5 to work through its other acceptance criteria (README, PLAYTEST_NOTES, DECISIONS tidy, verify green) and treating the tag itself as the one item needing a deliberate decision once everything else in 5.5 is ready.
- [2026-08-14] 5.5 — Ship review. `README.md` reviewed against TECH_SPEC.md §178's own checklist (what it is, run/build/verify/sim/perf/Android) — already complete, no changes needed. `PLAYTEST_NOTES.md` (written after Task 5.4, before any of Phase 7 landed) got a new "Update after Phase 7" section rather than a silent rewrite of the original text, so the history of what got fixed stays visible to a future reader: recorded that Task 7.10 closed the camera-shake/hit-feedback gap the original notes flagged as the single biggest feel gap, and Task 7.11 fixed the unreadable-Gate-numbers gap the original "Gates are the sharpest edge I hit" section implicitly suffered from. Re-verified 7.10 didn't regress across the rest of Phase 7 with a fresh headless-Chromium pass on the production build (`?debug=1`, a 120-Line, a 900-Blot debug wave, a real held-1.3s-then-released Flourish, then a Smear boss start) — zero console errors throughout; did not re-do 7.10's own frame-level shake-amplitude measurement (its dedicated unit tests plus that original verification already cover the actual math, and repeating it here would be re-deriving, not re-checking). Balance section of the notes left standing — current `BALANCE_REPORT.md` numbers (24.7% Strategy dominance, 38.1% First Seal reached) are within noise of what was already written, still both FAIL.
  `DECISIONS.md` read end-to-end for tidiness — consistent format throughout (date, decision, one-clause-or-more reason), no stray or contradictory entries found; left as-is. `npm run verify` green: 509 tests, typecheck/lint/build all clean.
  Tagged `v1.0.0` with 2 of 11 §11 rows still FAIL — a deliberate decision, not an oversight: Task 5.5's own acceptance bar doesn't name the §11 pass as a requirement (that's a separate CLAUDE.md "what 'done' means for the whole project" checklist item gating Phase 6's start, and Phase 6 stays locked either way per its own explicit rule), and three independent tuning attempts (Tasks 4.6, 4.6b, 7.9) have converged on needing a lever none of them has tried yet rather than another sweep of exhausted ones — holding the tag hostage to more of the same wouldn't fix the FAILs, only delay a milestone marker for no gain. Full reasoning in DECISIONS.md's 2026-08-14 entry.
  **Task 5.5 done. Phases 1-5 and Phase 7 are now both fully complete; `v1.0.0` is tagged locally.** The branch commit pushed cleanly (`git push origin claude/build-mxu0ej`), but `git push origin v1.0.0` fails with a consistent HTTP 403 (not a transient network error — four retries with backoff all failed identically) — this session's git credentials appear scoped to pushing the one designated branch, not creating arbitrary new refs like tags. The tag itself is real, annotated, and sitting on the exact commit that's on `origin/claude/build-mxu0ej` (`6b71136`); a session with broader push scope (or the user, locally) can push it with `git push origin v1.0.0` any time — no rebuild needed. Phase 6 remains explicitly locked per CLAUDE.md ("begin only when told") — nothing left to do in the unlocked backlog. Next session: check whether the user has unlocked Phase 6 before doing anything else; if not, try `git push origin v1.0.0` again in case credential scope changes, and otherwise the two remaining §11 FAILs are the only known open thread (Task 7.9's log already names the one untried lever).
