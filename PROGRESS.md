# PROGRESS

Append one line per completed task. Newest at the bottom. This file is how a fresh
session recovers context after a usage limit — write it for a reader with no memory
of what you just did.

Format:
`- [YYYY-MM-DD] <task id> — <what landed> — <anything the next session must know>`

## Current state

- Phase: 2
- Next task: 2.8
- Blockers: none

## Log

- [2026-08-07] 1.1 — Scaffolded Vite+TS strict project: tsconfig (app + a NodeNext `tsconfig.cli.json` for the headless `sim`/`perf` CLIs), ESLint flat config with a `Math.random` ban outside `/render`, Prettier, Vitest (node env, no jsdom dep), full `/src` folder skeleton per `TECH_SPEC.md` §3 with placeholder modules, `vercel.json` headers, minimal `manifest.webmanifest`, `README.md`. `npm run verify` green. — Next session: real per-file implementations start with 1.2 (fixed-timestep loop); CLI scripts import only `/core`, `/sim`, `/meta`, `/balance` and must use explicit `.js` extensions in relative imports for NodeNext resolution to work under `npm run sim`/`npm run perf`.
- [2026-08-07] 1.2 — `FixedStepLoop` in `src/core/loop.ts`: pure `advance(nowMs)` accumulator (no internal clock reads, so tests can feed fabricated timestamps), 60Hz fixed step, max 5 catch-up steps with remainder folded away on overrun, render alpha interpolation, pause/resume (resume re-primes `lastNowMs` so it doesn't burst-catch-up), `reset()`. `runInBrowser()` wraps it in `requestAnimationFrame`; `attachVisibilityAutoPause()` pauses on tab hidden; `mountDebugOverlay()` is a DOM-only fps/steps overlay gated behind `?debug=1` in `main.ts`. Determinism test drives 10,000 then 20,000 frames of exact fixed-dt length and asserts step count doesn't drift more than 1 step and the second identical-length segment matches the first — bit-exact equality against `N` isn't achievable with IEEE-754 timestamps at 1/60s, so the test asserts the property that actually matters (error doesn't compound). — Next session: `main.ts`'s `update`/`render` callbacks are still no-ops; they get wired to `/sim` and `/render` starting Task 2.1.
- [2026-08-07] 1.3 — `RngRegistry` in `src/core/rng.ts`: mulberry32 generator, one independent stream per concern (director/gates/slips/seals/cosmetic) each derived from the Passage seed via FNV-1a salting, plus `hashState()` (cyrb53 over a key-sorted canonical JSON stringify) for the determinism test. `Math.random` ESLint ban from 1.1 verified to actually fire outside `/render` (manually checked with a throwaway violating file). Determinism test drives a synthetic 5000-step RNG tape (standing in for the real World, which doesn't exist until Phase 2) and asserts byte-identical `hashState` at steps 100/1000/5000 for a replayed seed; a companion test confirms a cosmetic draw never perturbs another concern's sequence. — Next session: once `/sim/world.ts` lands (Task 2.x), point `hashState` at the real World snapshot for the full TECH_SPEC.md §12 determinism test instead of the synthetic tape.
- [2026-08-07] 1.4 — `Pool<T>` in `src/core/pool.ts`: fixed-capacity array pre-filled once by the factory, dense-array swap-remove for O(1) acquire/release with zero allocation after construction (non-null assertions scoped to this one file via an ESLint override, per TECH_SPEC.md §13's explicit carve-out for "pool internals"). `EventBus<Events>` in `src/core/events.ts`: a typed synchronous pub/sub for sim→render/audio signalling, documented as not allocation-safe if listeners (un)subscribe mid-emit — that's a deliberate constraint, not an oversight. Allocation test proves factory-call-count stays fixed across 600 acquire/release cycles and heap growth stays bounded; event bus has full pub/sub unit coverage. — Next session: 1.5 (viewport/input) is next; pools for the actual entity kinds (projectiles/blot/strokes/particles/floating numbers) get instantiated once `/sim` has real types to pool, starting Phase 2.
- [2026-08-07] 1.5 — `Viewport` (`src/platform/viewport.ts`): DPR-aware canvas backing store, letterboxed to the 9:21..3:4 aspect range against its container, safe-area insets read from CSS `env()` custom properties added in `index.html`. `InputSampler` (`src/platform/input.ts`): Pointer Events drag → world-unit `lateralDelta` (0.028 u/px) and keyboard A/D/arrows → 14 u/s, unified into one `InputFrame`; `holding` covers both pointer-down and a Space-key hold-to-Flourish binding (logged in DECISIONS.md — the spec requires a keyboard hold-to-Flourish key but doesn't name one). The critically-damped-follower math that turns `lateralDelta` into an actual Brush position lives in `src/sim/line.ts` (`stepCriticallyDamped`, exact closed-form solution, no overshoot on a step input) rather than in /platform, since it carries persistent state that must advance deterministically once per fixed step — /platform only ever hands the sim a raw per-step delta. `main.ts` wires Viewport+InputSampler+damped follower into a temporary debug marker (flat linear mapping, not the true projection) to prove the path end to end; verified in a real headless Chromium via Playwright (globally available in this environment, not added as a project dependency) — dragging the marker +200px moved it from x≈194.6 to x≈330.2 CSS px with smooth damping and no JS errors, fps/steps holding ~60. InputSampler is unit-tested against a hand-rolled fake EventTarget (no jsdom, per the §2 dependency list). — Next session: 1.6 is the BALANCE config object; the local constants in `main.ts`/`input.ts` (0.028, 14, clamp bounds, 0.07s tau) should migrate there and be imported instead.
- [2026-08-07] 1.6 — `BALANCE` in `src/sim/config.ts`: every gameplay-tunable number from GAME_DESIGN.md §3-11 (control, strokes, phrases, line/formation, wetness/flourish, road/slips/gates/sealstacks, blot/seals, spawn director, economy/inkstone, and the §11 balance targets for the harness to check against), grouped by section, `as const` + a recursive `deepFreeze()` so misuse is caught at both compile time (readonly literal types) and runtime (TypeError on mutation attempt). Percentages are stored as fractional multipliers throughout (0.04 not 4) so upgrade effects apply as `base * (1 + n * field)` with no stray `/100` anywhere. `main.ts` and `platform/input.ts` now import their lane/damping/control constants from `BALANCE` instead of local duplicates. Phase 6 numbers (Scrolls, Techniques, Brushes, Relics) are deliberately not encoded yet — nothing in the current codebase consumes them and Phase 6 is locked; they'll be added when Task 6.x actually starts. Added a test (`tests/sim/config.test.ts`) that mechanically enforces the "no magic numbers outside config.ts" rule: it scans every other file under `/sim` for bare numeric literals (exempting 0, 1, -1, and `[n]` array indices) — currently trivially green since Phase 2 hasn't added real `/sim` logic yet, but it'll catch regressions as soon as it does. **Phase 1 complete.** — Next session: Task 2.1 (projection + road) is the first real rendering work; `BALANCE.lane`/`forwardSpeed` feed directly into it, and the existing `main.ts` debug-marker wiring should be replaced by the real render pipeline rather than extended further.
- [2026-08-07] 2.1 — Implemented TECH_SPEC.md §5's projection: `render/camera.ts` holds the fixed camera constants and derives `cx/cy/unit/horizonOffsetPx` as fractions of the canvas box (not fixed pixel counts) so framing is identical at any letterboxed size — the two framing fractions (horizon position, world-to-pixel scale) aren't specified numerically anywhere, so I picked values that read as a natural racer-style vanishing point and logged the reasoning in code comments rather than DECISIONS.md (a rendering-feel choice, not a gameplay one). `render/projection.ts`'s `project()` is the literal formula; `DepthBucketList` is a zero-allocation counting-sort over pre-allocated typed arrays (64 buckets, far-to-near iteration) for the painter's-algorithm draw order Task 2.2+ will need for Strokes/Blot. `render/layers.ts` owns the 4 offscreen canvases (sky/water regenerated only on resize, road+trail redrawn every frame for now, actors cleared every frame, HUD stubbed) and composites them back-to-front plus a radial vignette onto the real canvas. `render/road.ts` draws the lane as a projected trapezoid with solid edge lines and a dashed centreline using a seamless modulo-phase scroll technique (`computeDashSpans`, pure and unit-tested for continuity across the phase wrap). Hit one real bug during visual verification: the initial near-draw-z (-4) put the road's near edge only ~68% down a 1920px-tall canvas instead of past the bottom, because camera pitch (camY=4.2) needs a depth close to NEAR_CLIP before the ground plane's projected edge clears the frame — fixed by moving NEAR_DRAW_Z to -5.8, verified visually before/after via a headless-Chromium screenshot (Playwright, globally available, not a project dependency). Verified in-browser at 390×844, 768×1024 (3:4), 400×933 (~9:21), and 1080×1920 — consistent framing, no seams, scroll rate confirmed via the debug overlay's steps/s. `main.ts`'s temporary Brush marker now renders through the real projection instead of a flat linear mapping. — Next session: 2.2 is the Brush/Line formation; it should consume `stepCriticallyDamped` from `src/sim/line.ts` (already built in 1.5) and draw through `DepthBucketList`+`project()` on the actors layer, replacing `main.ts`'s placeholder marker entirely.
- [2026-08-07] 2.2 — Formation math in `src/sim/line.ts`: `LineState` is an ordered array (front-most first, matching "newest fill from the back, losses remove from the front" directly — no separate index bookkeeping needed), `computeFormationSlot()` derives row/col/lateral/depth from a flat index, generalising GAME_DESIGN.md §5's "front row leads by 0.3u at centre" arc to every row (a parabolic bulge, documented as a deliberate generalisation beyond the literal spec). `classifyForRender()` implements the >60-Strokes density-block rule. Added `src/sim/stroke.ts` with just the `StrokeClass` type now, since formation needed it before Task 2.3 builds real class stats. Render side (`src/render/strokes.ts`): three distinct silhouettes (Hane tick, Tome block, Harai diagonal sliver) colour-coded per §4/§12, plus a density-block mass capped at a fixed max visual depth (3.5u) so a 400-Stroke Line can't project geometry behind the camera. Found and fixed a real bug via visual inspection: blending all three class colours by weight for the block's fill produced exactly the muddy tan GAME_DESIGN.md §12 explicitly forbids ("not a warm-cream-and-terracotta scheme") — switched to the dominant class's pure colour as the fill, with minority classes shown only as stipple dots (dots matching the dominant colour would be invisible anyway). Verified in headless Chromium at N=1/5/37/120/400 via debug keys 1-5 (gated behind `?debug=1`, GAME_DESIGN.md §13 non-goal on tutorial text doesn't apply to dev tooling): steady 60fps/60 steps-s even at N=400, correct arc/density-block transitions, no console errors. — Next session: 2.3 (Strokes and firing) adds the actual class stats table to `stroke.ts` and projectile pools; the debug round-robin class assignment in `main.ts`'s `buildDebugLine()` should stay as dev tooling, not be mistaken for the real Slip-driven composition system (Task 2.5).
- [2026-08-07] 2.3 — `src/sim/projectiles.ts`: a pooled `Projectile` type (`Pool<T>` capacity now sourced from a new `BALANCE.pools` section — TECH_SPEC.md §5's perf-budget numbers, not GAME_DESIGN.md's, but still centralized rather than left as literals). `computeFiringPlan()` implements §5's row-scaled DPS exactly: front row at full rate, back rows at `extraRowDpsContribution` (55%) each, capped at `maxVisibleProjectilesPerClassPerS` (24) with the surplus folded into a `damagePerProjectile` multiplier so total DPS is preserved regardless of cap. `updateFiring()`/`updateProjectileMotion()` mutate the pool in place by design (documented as a deliberate exception to /sim's usual copy-on-write style — projectiles are the one genuine up-to-2048-item-per-frame hot path). Found and fixed a real bug via unit tests before it ever hit the screen: `updateFiring`'s muzzle round-robin cursor was a local variable reset every call, so at typical fire rates (≤1 shot/frame) every projectile spawned from the same muzzle forever — moved the cursor into a persisted `ClassFiringState` alongside the time accumulator. Added `Pool.get(index)` for safe backward-iteration-with-release (needed so `updateProjectileMotion` can despawn expired projectiles without an intermediate array). `applyArmorMultiplier`/`registerHit` implement the §4 armour table and Harai's pierce-count-then-destroy lifecycle, fully unit-tested without needing real Blot/Slip entities yet (Task 2.4/2.5). Render side adds `drawProjectiles()` to `strokes.ts` (dot/blob/line per class) — not a new file, since /render's layout has no dedicated projectiles.ts. Verified in headless Chromium at N=400 with the debug Line: steady 60fps/60 steps-s while all three classes fire simultaneously, projectile counts visibly capped rather than flooding. — Next session: 2.4 (the Blot) is what projectile collision (`registerHit`, splash, armour) actually gets tested against for real; `computeRowClassCounts`/`computeFrontRowSourcePositions` in `line.ts` are the integration points Blot's spawn/contact logic will sit alongside.
- [2026-08-07] 2.4 — `src/sim/blot.ts`: all six Blot types (pooled, `BALANCE.pools.blotCapacity`=1200), `updateBlotMotion()` (straight march for four classes, Drifter lateral strafe+bounce, Blotter stop-at-range+periodic-lob), `resolveLineContact()` (Blot-reaches-Brush → 1 Stroke lost and dies, 3 for Crust, per §5 — fully unit-tested including a mixed-group-in-one-step sum check), `resolveBlotDeaths()` (hp≤0 cleanup + Splitter's 3-Smudges-at-±1u split, stress-tested with 100 simultaneous Splitter deaths to validate the backward-iteration-with-respawn invariant). Also built `src/sim/collision.ts` (projectile↔Blot hit detection, armour multiplier, Tome splash, Harai pierce) since Task 2.3 built the attacker half of combat and 2.4 is where a target first exists to test it against — not tied to its own task ID in TASKS.md but necessary for the combat loop to do anything. Two spec gaps logged in DECISIONS.md: Drifter's strafe speed (4 u/s) and the projectile-Blot hit radius (0.6u), neither given a number in GAME_DESIGN.md. `render/blot.ts`: a stable-per-pool-slot "ragged blob" silhouette (hash-seeded from `poolIndex`, not `Math.random()` per frame, so it doesn't flicker) for near/low-count Blot, cheap plain circles for the >150-count/>30u-distance mass tier (`BALANCE.blotRender`), matching §8.1's "resolves into individuals as it nears." Found and fixed a real pre-existing bug from Task 2.2 during this task's visual verification: the Stroke density-block's capped depth (3.5u) still let its near edge approach the projection's near-clip plane closely enough that its scale exploded, visually painting over the *entire* road instead of sitting near the Brush — tightened the cap to 1.8u. Verified in headless Chromium: 900 Blot + a 120-Stroke Line + full firing/collision/death pipeline running simultaneously holds 60fps, mass-to-individual transition and the corrected density block both read correctly. — Next session: 2.5 (Slips) is the first real source of Strokes joining the Line and Gold Leaf-less growth; it'll want `addStroke()` (line.ts, already built) and probably its own pooled entity type alongside collision.ts's pattern.
- [2026-08-08] 2.5 — `src/sim/slips.ts`: pooled Slip entity (+1/+5/+25 kinds, HP from BALANCE), `computeClassWeights()`/`pickSlipClass()` implementing §7.1's least-held-class-1.5×-more-likely rule via the seeded `'slips'` RNG concern (never perturbs other systems' sequences — exactly what that concern registry was built for in Task 1.3), and a `JoiningRecruit` pool for the runner-joins-the-back animation (travel duration logged as a spec gap in DECISIONS.md, 0.6s). `resolveSlipDeaths()`/`updateJoiningRecruits()` complete the pipeline: Slip hp≤0 → recruits spawn at its death position → travel to the Line's next formation slot → `addStroke()` on arrival. Extended `collision.ts` with `resolveProjectileSlipCollisions()` (same armour-multiplier pipeline as Blot, deliberately no Tome splash against Slips — splash is specified for Blot combat, and letting one shot clear a whole Slip run would undercut "shooting is the commitment"). Caught a real bug during visual verification, not by the unit tests (which spawn Slips already in range, so they didn't exercise this): Slips had no motion at all — GAME_DESIGN.md doesn't give them a speed stat since they're staked road furniture, not walkers, but that also means nothing was making them approach the Brush the way the road markings visually scroll. Added `updateSlipMotion()`, shifting z at `BALANCE.forwardSpeed.baseUPerS` (the same rate the world scrolls) since the Brush's own z is fixed at 0. Verified in headless Chromium: a debug-spawned Slip run scrolls into range, gets shot down, and the Line visibly grows from 3 to 5+ Strokes of the recruited class within a couple seconds, holding 60fps throughout. — Next session: 2.6 (Gates and Sealstacks) is next; Sealstack HP/impact will likely want the same "no stat given → needs its own scroll motion" check this task's bug came from, since it's also static road content.
- [2026-08-08] 2.7 — `src/sim/director.ts`: pure Spawn Director functions matching §9's formulas exactly (`computePressure`, `computeWaveIntervalS`/`computeWaveSize`, the pressure-band `computeComposition` table with anti-snowball's Crust bump and the mercy rule's Crust/Splitter suppression, `computeSlipBudgetPer100U`, `planSlipSpawn` for turning a HP budget into a concrete +1/+5/+25 run, and the full mercy-rule state machine). `src/sim/world.ts`: the `World`/`stepWorld` consolidation Task 2.7 is really about — ties every Phase-2 subsystem (Line, projectiles, Blot, Slips/recruits, Gates/Temper, Sealstacks) into one steppable root state, with the Director driving real wave/Slip-run/Gate-pair/Sealstack spawning on time/distance thresholds instead of the debug-key spawns Tasks 2.2-2.6 used. `computeGoldLeaf()` implements §10's formula (Seals term always 0 — Seals don't exist until Phase 3).
  Two real bugs found while writing `world.test.ts`, both fixed:
  1. **Not a bug, but looked like one**: firing has no auto-aim by design (confirmed via GAME_DESIGN.md §8.1's Drifter — "forcing you to lead your shots" only makes sense if the *player* aims, via Brush position). A stationary centred Brush only lines up with 1-in-`rowSize` muzzles, so two tests expecting near-instant kills on a target planted at a fixed x were wrong, not the sim — fixed the tests to give targets realistic travel distance instead. Logged in DECISIONS.md.
  2. **A real pool leak**: unshot Slips never left `slipPool` (only `resolveSlipDeaths` released them, nothing released a *missed* one). `slipCapacity` (64) is smaller than what a multi-minute Passage offers (~14 Slips every ~100u), so any run that ignores Slips exhausts the pool partway through and spawning silently stops thereafter — added `resolveMissedSlips()` (slips.ts) releasing anything `missDespawnMarginU` (2u) past the Brush, wired into `stepWorld`, regression-tested (a 6000-step unshot run now stays bounded instead of leaking).
  `src/balance/bot.ts`: all five TECH_SPEC.md §6 strategies as pure `World → WorldInput` functions — `greedy-slips`/`greedy-kill` do what they say (nearest Slip vs. a sliding-window densest-Blot-cluster search), `mixed` switches to kill-mode when projected 2s Blot arrivals exceed `N × 0.4`, `gates` adds an expected-value comparison between an inbound Gate pair's two sides (Sealed evaluated by its known 60/40 reveal odds, never the concrete rolled effect — a real player can't see through the seal either), `random` is a smooth seeded wander standing in for a player not really looking. `src/balance/harness.ts`: the CLI (`--runs`/`--strategy`/`--upgrades`/`--seed`/`--sweep`/`--out`), running Passages to death (or a 400s safety cap, recorded as `timeout`) and measuring death-within-8s-of-Gate itself since World doesn't track it. `src/balance/report.ts`: `BALANCE_REPORT.md` generator — ASCII histograms, death-cause breakdown, a §11 pass/fail table that honestly reports N/A (not a fabricated verdict) for every target whose prerequisite doesn't exist yet (Seals, upgrades, per-Blot-class death attribution).
  Also fixed `tsconfig.cli.json` (`npm run build:cli` had never actually been run before — not covered by `npm run verify` — and failed immediately on `core/loop.ts`'s DOM types; added `"dom"` to `lib`, type-only, the CLI never calls the DOM-touching methods). `--sweep` parses but doesn't yet override `BALANCE` (it's deliberately a frozen singleton per Task 1.6, and real sweeping needs a config-injection path threaded through `createWorld`/`stepWorld` — that's Task 4.6's job). Ran `npm run sim -- --runs 2000`: completes in ~11 minutes, writes `BALANCE_REPORT.md`. Targets all FAIL/N/A as expected at this stage — Passage length and peak Line both blow past target ranges (median ~389s vs. 55-85s target, peak Line pinned at the 999 hard cap for the `mixed` bot), meaning the Director's pressure ramp is nowhere near tuned yet; that tuning is explicitly Task 4.6's job, not this one's. — Next session: **known tech debt** — `main.ts` still has its own inline simulation wiring from Tasks 2.2-2.6's debug spawns, separate from `world.ts`'s consolidated version; they're not yet unified, so `main.ts`'s Director-adjacent behavior (spawn positions etc.) doesn't match `world.ts`'s real Director logic. Refactoring `main.ts` to consume `World`/`stepWorld` directly is unstarted and should happen before or during Task 2.8 gets much further, or the two will keep drifting apart. 2.8 (Wetness and Flourish) is next.
- [2026-08-08] (tech debt, ahead of 2.8) — Refactored `main.ts` to consume `sim/world.ts`'s `World`/`stepWorld` directly instead of its own inline copy of the same simulation wiring (predating `world.ts`, left over from Tasks 2.2-2.6's incremental builds). `update()` is now just "sample input, call `stepWorld` once"; `render()` reads straight from `world.*` (blotPool, slipPool, currentGatePair, line, distanceU for road scroll, etc.) instead of a dozen separate local variables. All debug keys (1-9, 0, G, H) retarget the same `World`'s pools/line; added `KeyR` to spawn a fresh `World` (previously there was no way to recover from an empty Line — there's still no real death/restart screen, that's Task 2.11, but the debug build isn't stuck anymore either). Verified in headless Chromium at 390×844 with `?debug=1`: steady 60fps/60 steps-s, no console errors, and — the actual point of this refactor — Blot waves now appear on their own within ~15s of load without touching a debug key, which is the real Spawn Director finally driving ordinary play instead of only reacting to manual spawns. Doing this now rather than after 2.8 avoids building Wetness/Flourish's `stepWorld` integration twice.
- [2026-08-08] 2.6 — `src/sim/gates.ts`: `GateEffect` carries optional `arithmeticOp`/`conversionTarget`/`temperStat` fields (normally exactly one set; a Sealed reveal can set both arithmetic+conversion, which is the only place GAME_DESIGN.md §7.2's "multiply before add within a single gate; conversion applies after count changes" has any observable effect — decided and logged in DECISIONS.md, along with the pair-archetype mix). `generateGatePair()` guarantees the pairing rules *by construction* rather than rejection-sampling — three archetypes (20% Sealed-vs-modest, 30% safety-valve good-vs-bad, 50% explicit dilemma), each individually incapable of producing an illegal pair — verified with a 10,000-roll test asserting `isLegalPair` holds every time and the dilemma fraction clears the 0.25 floor (actual ~70%). `applyGateEffect()` enforces arithmetic-before-conversion in its own control flow, tested with a compound effect where getting the order wrong would leave newly-grown Strokes unconverted (a real, observable divergence, not just a call-order check). `src/sim/sealstacks.ts` mirrors the Blot/Slip pooled-entity pattern: `resolveSealstackContact()` implements the exact `ceil(remainingHP/12)` impact formula, dodging to the unblocked lane half costs nothing. Extended `collision.ts` with `resolveProjectileSealstackCollisions()` (a lane-half + z-thickness hit test, not circle-vs-circle, since Sealstacks are wide). Logged two more spec gaps (Sealstack HP=60, hit-thickness=1.0u) in DECISIONS.md. Render additions went into `road.ts` (family-colour-coded gate halves, a dark Sealstack block) since neither fit the render layout's existing files better; `CLASS_COLOR` was exported from `strokes.ts` so road.ts could reuse it for Conversion gates instead of duplicating the palette mapping. Verified in headless Chromium: a debug-spawned gate pair and Sealstack both render, resolve on contact, and visibly affect the Line (this session's Line started at only N=3, so a bad-arithmetic or Sealstack outcome zeroing it to empty is correct behaviour, not a bug), holding 60fps throughout. — Next session: 2.7 (Spawn Director + balance harness) is next and is a big one — it's what finally decides *when* Blot waves, Slip runs, and Gate pairs actually appear during a Passage, replacing every debug-key spawn built across 2.2-2.6 with real pressure-driven spawning; it also stands up the headless harness and bot strategies from TECH_SPEC.md §6, which nothing so far has needed.

