<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- TECH_SPEC.md §7: "No image files." The default Capacitor scaffold points this at
         a set of placeholder splash-screen PNGs (one per density/orientation) — replaced
         with a plain solid-colour background (PALETTE.deep, colors.xml) instead, since a
         flat colour needs no image asset at all and matches the game's own background
         the instant the WebView takes over. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@color/colorPrimaryDark</item>
    </style>
</resources>